# Vingo P2 Implementation Plan

> **For agentic workers:** Theo CLAUDE.md của repo, plan này được **tự triển khai trực tiếp trong phiên** (không subagent-driven-development, không TDD/verification skill của superpowers). Steps dùng checkbox (`- [ ]`) để theo dõi.

**Goal:** Người học dùng Vingo với video local (không cloud), YouTube, chia sẻ community, PWA có trang offline và trang học dùng tốt trên điện thoại (cả xoay ngang).

**Architecture:** Thêm provider `local` xuyên suốt hợp đồng ingest (shared zod → core → mcp guard → skill) và web chọn file bằng object URL. Tách khối video thành `VideoFrame` cắm `PlayerAdapter` theo provider (`Html5PlayerAdapter` / `YouTubePlayerAdapter`); StudyView giữ state như cũ. Share community chỉ là UPDATE `visibility` trên RLS có sẵn. Service worker viết tay, tĩnh trong `public/`.

**Tech Stack:** Next.js 15 (Turbopack), React 19, Tailwind v4, Supabase (RLS, pgTAP), zod, vitest + testing-library (jsdom), Playwright, YouTube IFrame API, File System Access API, Service Worker API.

**Spec:** `docs/superpowers/specs/2026-09-08-vingo-p2-design.md` (P2-1 → P2-8). Spec master: `docs/superpowers/specs/2026-08-28-vingo-design.md` §6.

## Global Constraints

- Node ≥ 22.5, pnpm monorepo; chạy lệnh từ root. `pnpm -r test`, `pnpm --filter web typecheck`, `supabase test db`, `pnpm e2e` phải xanh sau mỗi khối.
- **Không chạy `pnpm --filter web build` khi dev server đang chạy** (dùng chung `.next`). Task 14 (PWA) cần build: dừng dev server trước.
- Migration đánh số `000N_*.sql`, không sửa migration đã áp. RLS đổi → pgTAP.
- Không hard-code màu; dùng token (`bg-page`, `text-ink`, `border-line`, `bg-surface`, `bg-surface-2`, `text-muted`, `bg-accent`, `bg-accent-soft`, `text-accent`, `bg-warning-soft`, `text-warning`, `bg-danger-soft`, `text-danger`).
- Chuỗi tiếng Nhật: `lang="ja"` + `font-jp`. `cn()` chỉ nối chuỗi — không truyền `hidden` vào component đã có display class.
- Desktop và mobile là **một cây DOM**, phân nhánh bằng `lg:` (và variant `land:` thêm ở Task 13).
- Component test: `// @vitest-environment jsdom` đầu file, `@testing-library/react`.
- E2E dùng `data-testid`, idempotent (trả lại trạng thái đã đổi). Login helper: `cat@vingo.local` / `devpass123`.
- Commit message tiếng Anh, prefix `feat/fix/test/chore/docs`; mô tả trong code tiếng Việt.
- Không tier/billing, không OPFS, không deploy, không serwist.

---

## Khối A — Video local

### Task 1: Migration `0009_local_video.sql` + pgTAP

**Files:**
- Create: `supabase/migrations/0009_local_video.sql`
- Create: `supabase/tests/007_local_video.sql`

**Interfaces:**
- Produces: `lessons.video_provider` nhận `'local'`; cột `lessons.video_size_bytes bigint null`.

- [x] **Step 1: Viết migration**

```sql
-- 0009_local_video.sql — P2: provider 'local' (video không lên cloud) + kích cỡ file để web kiểm tra khớp.
alter table public.lessons drop constraint lessons_video_provider_check;
alter table public.lessons add constraint lessons_video_provider_check
  check (video_provider in ('storage','youtube','local'));
alter table public.lessons add column video_size_bytes bigint;
comment on column public.lessons.video_size_bytes is 'Kích cỡ file video gốc (byte). Chỉ để web cảnh báo khi người học chọn nhầm file local.';
```

- [x] **Step 2: Viết pgTAP**

```sql
-- supabase/tests/007_local_video.sql
begin;
select plan(3);

insert into auth.users (instance_id, id, aud, role, email, encrypted_password, email_confirmed_at, raw_app_meta_data, raw_user_meta_data, created_at, updated_at) values
 ('00000000-0000-0000-0000-000000000000','11111111-0000-4000-8000-000000000001','authenticated','authenticated','owner@t.local','',now(),'{}','{}',now(),now());

select set_config('request.jwt.claims','{"sub":"11111111-0000-4000-8000-000000000001","role":"authenticated"}', true);
set local role authenticated;

select lives_ok(
  $$insert into public.lessons (id, owner_id, title, source_type, source_lang, target_lang, video_provider, video_ref, duration_sec, video_size_bytes, status)
    values ('aaaa0000-0000-4000-8000-00000000000a','11111111-0000-4000-8000-000000000001','bài local','zoom','ja','vi','local','meeting.mp4',2617,734003200,'ready')$$,
  'owner tạo được bài video local');
select is((select video_size_bytes from public.lessons where id='aaaa0000-0000-4000-8000-00000000000a'), 734003200::bigint, 'lưu video_size_bytes');
select throws_ok(
  $$insert into public.lessons (id, owner_id, title, source_type, video_provider)
    values ('bbbb0000-0000-4000-8000-00000000000b','11111111-0000-4000-8000-000000000001','sai provider','zoom','dropbox')$$,
  '23514', null, 'provider lạ bị check constraint từ chối');

select * from finish();
rollback;
```

- [x] **Step 3: Áp migration + chạy test**

Run: `supabase db reset && supabase test db`
Expected: 007 pass 3/3, các file cũ vẫn pass.

- [x] **Step 4: Commit**

```bash
git add supabase/migrations/0009_local_video.sql supabase/tests/007_local_video.sql
git commit -m "feat(db): allow video_provider 'local' and add lessons.video_size_bytes"
```

### Task 2: Hợp đồng ingest — shared zod, web types, core

**Files:**
- Modify: `packages/shared/src/lesson.ts:31-33`
- Modify: `packages/shared/src/lesson.test.ts`
- Modify: `packages/core/src/lessons.ts:38-45,122-137`
- Modify: `packages/core/src/_testutil.ts:56-60,73`
- Modify: `packages/core/src/lessons.test.ts`
- Modify: `apps/web/src/lib/types.ts:1-8`

**Interfaces:**
- Produces: `LessonJson.video_provider: "storage" | "youtube" | "local"`, `LessonJson.video_size_bytes?: number`; `LessonRow.video_provider` thêm `"local"`, `LessonRow.video_size_bytes: number | null`.

- [x] **Step 1: Shared schema**

Trong `packages/shared/src/lesson.ts` thay 2 dòng:
```ts
  video_provider: z.enum(["storage", "youtube", "local"]).optional(),
  video_ref: z.string().optional(),
  duration_sec: z.number().int().positive().optional(),
  video_size_bytes: z.number().int().positive().optional(),
```

- [x] **Step 2: Shared test** — thêm vào `lesson.test.ts` cạnh case youtube:

```ts
  it("local: không cần video file, nhận video_size_bytes", () => {
    const local = { ...valid, source_type: "zoom", video_provider: "local", video_ref: "meeting.mp4", duration_sec: 2617, video_size_bytes: 734003200, vocab: [] };
    const parsed = lessonJsonSchema.parse(local);
    expect(parsed.video_provider).toBe("local");
    expect(parsed.video_size_bytes).toBe(734003200);
  });
  it("local không có video_ref vẫn hợp lệ", () => {
    const { video_ref: _, ...rest } = { ...valid, video_provider: "local" as const, vocab: [] };
    expect(lessonJsonSchema.parse(rest).video_ref).toBeUndefined();
  });
  it("video_size_bytes phải là số nguyên dương", () => {
    expect(() => lessonJsonSchema.parse({ ...valid, video_size_bytes: -1 })).toThrow();
  });
```

Run: `pnpm --filter @vingo/shared test` → PASS.

- [x] **Step 3: Core** — `packages/core/src/lessons.ts`:

```ts
function videoNextStep(lesson: LessonJson): string {
  if (lesson.video_provider === "youtube") return "youtube — không cần video, bài đã sẵn sàng.";
  if (lesson.video_provider === "local")
    return "local — không upload; bài đã sẵn sàng. Người học mở bài trên web, bấm 'Chọn video trên máy' và trỏ tới file gốc.";
  return [
    "Cần đưa video để bài sang ready:",
    "• Đường A (ưu tiên): có direct-download link → ingest_video_from_url.",
    "• Đường B: file local → request_video_upload → curl PUT → finalize_lesson.",
  ].join("\n");
}
```
và trong `lessonRow`:
```ts
  const readyNow = lesson.video_provider === "youtube" || lesson.video_provider === "local";
  return {
    ...
    duration_sec: lesson.duration_sec ?? null,
    video_size_bytes: lesson.video_size_bytes ?? null,
    status: readyNow ? "ready" : "draft",
    ingest_error: null,
  };
```
Sửa comment phía trên hàm: `// youtube/local → ready ngay; còn lại → draft.`

- [x] **Step 4: Core test util + test** — `_testutil.ts`: `video_provider?: "storage" | "youtube" | "local"; video_size_bytes?: number;` và truyền `video_size_bytes: o.video_size_bytes` vào object. Thêm test trong `lessons.test.ts` sau case youtube:

```ts
  it("local → status ready ngay, lưu video_size_bytes", async () => {
    const ctx = ctxFor(userA);
    const lesson = makeLesson({ video_provider: "local", video_ref: "meeting.mp4", video_size_bytes: 1234567 });
    const res = await createLesson(ctx, lesson);
    expect(res.video_next_step).toContain("local");
    expect((await getIngestStatus(ctx, lesson.id)).status).toBe("ready");
    const { data } = await admin.from("lessons").select("video_size_bytes").eq("id", lesson.id).single();
    expect(data!.video_size_bytes).toBe(1234567);
  });
```

Run: `pnpm --filter @vingo/core test` (cần Supabase local đang chạy) → PASS.

- [x] **Step 5: Web types** — `apps/web/src/lib/types.ts`:

```ts
  video_provider: "storage" | "youtube" | "local" | null; video_ref: string | null;
  duration_sec: number | null; video_size_bytes: number | null; thumb_path: string | null;
```

Run: `pnpm --filter web typecheck` → PASS.

- [x] **Step 6: Commit**

```bash
git add packages/shared packages/core apps/web/src/lib/types.ts
git commit -m "feat(core): local video provider is ready immediately; video_size_bytes in lesson contract"
```

### Task 3: MCP guard + authoring guide + skill

**Files:**
- Modify: `apps/mcp/src/tools/index.ts:56-66,130-135,170-175,191-197`
- Modify: `packages/core/content/authoring-guide.md:25-27`
- Modify: `skills/study-kit/SKILL.md:40-58`

- [x] **Step 1: `verifyOwned` trả provider, 3 tool video từ chối bài local**

```ts
  const LOCAL_VIDEO_MSG =
    "Bài này là video local (không nhận video lên cloud). Muốn lưu cloud: tạo lại bài với video_provider 'storage'.";

  async function verifyOwned(lessonId: string): Promise<{ ownerId: string; status: string; provider: string | null }> {
    const { data, error } = await ctx.supabase
      .from("lessons")
      .select("owner_id, status, video_provider")
      .eq("id", lessonId)
      .maybeSingle();
    if (error) throw new Error(error.message);
    if (!data || data.owner_id !== userId) throw new Error("Không tìm thấy bài hoặc bài không thuộc về bạn.");
    return { ownerId: data.owner_id, status: data.status, provider: data.video_provider };
  }
```
Trong `ingest_video_from_url`, `request_video_upload`, `finalize_lesson`, ngay sau `verifyOwned`:
```ts
        const { ownerId, provider } = await verifyOwned(lesson_id);
        if (provider === "local") return errText(LOCAL_VIDEO_MSG);
```

Run: `pnpm --filter mcp typecheck && pnpm --filter mcp test` → PASS.

- [x] **Step 2: Authoring guide** — sửa 2 dòng schema và thêm ghi chú:

```
  "video_provider": "youtube",     // optional: 'storage' | 'youtube' | 'local'
  "video_ref": "dQw4w9WgXcQ",      // youtube: video id · local: tên file gốc (gợi ý cho người học) · storage: bỏ trống
  "duration_sec": 1830,            // optional; local: nên có (web dùng cảnh báo chọn nhầm file)
  "video_size_bytes": 734003200,   // optional; local: kích cỡ file gốc nếu biết
```
Thêm sau khối ví dụ hoàn chỉnh (trước `## 2.`):
```
### Video local (`video_provider: "local"`)
Video KHÔNG upload; bài `ready` ngay khi create_lesson. Người học mở bài trên web và tự chọn file
trên máy. Điền `video_ref` = tên file, `duration_sec` (bắt buộc có nếu nguồn cho biết),
`video_size_bytes` nếu biết. KHÔNG gọi ingest_video_from_url / request_video_upload / finalize_lesson.
```

- [x] **Step 3: Skill bước 6** — thay đầu mục **Video** trong `SKILL.md`:

```
6. **Video**:
   - **YouTube**: xong luôn (không có video file, bài đã `ready`).
   - **Còn lại (Zoom / link / file local)**: hỏi user ĐÚNG 1 câu trước khi create_lesson:
     > Video này để **local** (mặc định — không upload, khi học sẽ chọn file trên máy) hay **lưu cloud**?
     - **Local (mặc định, user không trả lời → local)**: điền ngay trong `create_lesson`
       `video_provider: "local"`, `video_ref: "<tên file gốc>"`, `duration_sec` (Zoom: từ metadata
       recording; video-download-mcp: `get_video_info`; file local: hỏi user hoặc bỏ trống),
       `video_size_bytes` nếu biết. KHÔNG gọi tool video nào. Báo `web_url` + nhắc: "mở bài,
       bấm *Chọn video trên máy* và trỏ tới file `<tên>`". Bỏ qua bước 7.
     - **Cloud**: giữ `video_provider: "storage"` và đi đường A/B bên dưới.
   - **Đường A (ưu tiên)** — ... (giữ nguyên)
```
Lưu ý: bước 5 `create_lesson` hiện đứng trước bước 6 — đổi thứ tự: câu hỏi local/cloud nằm ở bước 4 (khi dựng JSON), ghi rõ "quyết định provider trước khi gọi create_lesson".

- [x] **Step 4: Commit**

```bash
git add apps/mcp/src/tools/index.ts packages/core/content/authoring-guide.md skills/study-kit/SKILL.md
git commit -m "feat(mcp): reject video tools on local-video lessons; guide and skill ask local vs cloud"
```

### Task 4: `lib/local-video.ts` (thuần + unit test)

**Files:**
- Create: `apps/web/src/lib/local-video.ts`
- Create: `apps/web/src/lib/local-video.test.ts`

**Interfaces:**
- Produces:
  ```ts
  export function supportsFileHandles(w?: Window): boolean
  export function checkMatch(file: { size: number }, durationSec: number | null, lesson: Pick<LessonRow, "duration_sec" | "video_size_bytes">): { ok: boolean; reasons: string[] }
  export async function saveHandle(lessonId: string, handle: FileSystemFileHandle): Promise<void>
  export async function loadHandle(lessonId: string): Promise<FileSystemFileHandle | null>
  export async function forgetHandle(lessonId: string): Promise<void>
  ```

- [x] **Step 1: Test**

```ts
// apps/web/src/lib/local-video.test.ts
// @vitest-environment jsdom
import { describe, expect, it } from "vitest";
import "fake-indexeddb/auto";
import { checkMatch, forgetHandle, loadHandle, saveHandle, supportsFileHandles } from "./local-video";

describe("checkMatch", () => {
  const lesson = { duration_sec: 2617, video_size_bytes: 1000 };
  it("khớp khi lệch duration ≤ 2s và size bằng", () => {
    expect(checkMatch({ size: 1000 }, 2618.4, lesson)).toEqual({ ok: true, reasons: [] });
  });
  it("cảnh báo khi duration lệch > 2s", () => {
    const r = checkMatch({ size: 1000 }, 2500, lesson);
    expect(r.ok).toBe(false);
    expect(r.reasons[0]).toMatch(/thời lượng/);
  });
  it("cảnh báo khi size khác", () => {
    expect(checkMatch({ size: 999 }, 2617, lesson).reasons[0]).toMatch(/kích cỡ/);
  });
  it("bỏ kiểm tra trường lesson không có", () => {
    expect(checkMatch({ size: 5 }, 10, { duration_sec: null, video_size_bytes: null }).ok).toBe(true);
    expect(checkMatch({ size: 5 }, null, lesson).reasons).toEqual(["kích cỡ file khác bản gốc"]);
  });
});

describe("supportsFileHandles", () => {
  it("false khi không có showOpenFilePicker", () => { expect(supportsFileHandles({} as Window)).toBe(false); });
  it("true khi có", () => { expect(supportsFileHandles({ showOpenFilePicker: () => {} } as unknown as Window)).toBe(true); });
});

describe("handle store", () => {
  it("save → load → forget", async () => {
    const fake = { name: "a.mp4" } as unknown as FileSystemFileHandle;
    await saveHandle("lesson-1", fake);
    expect(await loadHandle("lesson-1")).toEqual(fake);
    await forgetHandle("lesson-1");
    expect(await loadHandle("lesson-1")).toBeNull();
  });
});
```
Cài dev dep: `pnpm --filter web add -D fake-indexeddb`.

- [x] **Step 2: Implement**

```ts
// apps/web/src/lib/local-video.ts — video_provider 'local' (spec P2 §2.6): thuần, không React.
import type { LessonRow } from "./types";

const DURATION_TOLERANCE_SEC = 2;
const DB_NAME = "vingo-local-video";
const STORE = "handles";

export function supportsFileHandles(w: Window | undefined = typeof window === "undefined" ? undefined : window): boolean {
  return !!w && "showOpenFilePicker" in w;
}

/** So file người học chọn với metadata bài. Chỉ cảnh báo, không chặn. */
export function checkMatch(
  file: { size: number },
  durationSec: number | null,
  lesson: Pick<LessonRow, "duration_sec" | "video_size_bytes">,
): { ok: boolean; reasons: string[] } {
  const reasons: string[] = [];
  if (lesson.duration_sec != null && durationSec != null && Math.abs(durationSec - lesson.duration_sec) > DURATION_TOLERANCE_SEC)
    reasons.push("thời lượng lệch bản gốc");
  if (lesson.video_size_bytes != null && file.size !== lesson.video_size_bytes)
    reasons.push("kích cỡ file khác bản gốc");
  return { ok: reasons.length === 0, reasons };
}

function openDb(): Promise<IDBDatabase> {
  return new Promise((resolve, reject) => {
    const req = indexedDB.open(DB_NAME, 1);
    req.onupgradeneeded = () => req.result.createObjectStore(STORE);
    req.onsuccess = () => resolve(req.result);
    req.onerror = () => reject(req.error);
  });
}
function tx<T>(mode: IDBTransactionMode, run: (s: IDBObjectStore) => IDBRequest<T>): Promise<T> {
  return openDb().then((db) => new Promise<T>((resolve, reject) => {
    const req = run(db.transaction(STORE, mode).objectStore(STORE));
    req.onsuccess = () => { resolve(req.result); db.close(); };
    req.onerror = () => { reject(req.error); db.close(); };
  }));
}
export const saveHandle = (lessonId: string, h: FileSystemFileHandle) => tx("readwrite", (s) => s.put(h, lessonId)).then(() => undefined);
export const loadHandle = (lessonId: string) => tx<FileSystemFileHandle | undefined>("readonly", (s) => s.get(lessonId)).then((v) => v ?? null);
export const forgetHandle = (lessonId: string) => tx("readwrite", (s) => s.delete(lessonId)).then(() => undefined);
```

Run: `pnpm --filter web test -- src/lib/local-video.test.ts` → PASS.

- [x] **Step 3: Commit**

```bash
git add apps/web/src/lib/local-video.ts apps/web/src/lib/local-video.test.ts apps/web/package.json pnpm-lock.yaml
git commit -m "feat(web): local video helpers (match check, file handle store)"
```

### Task 5: `LocalVideoSource` + nối vào StudyView + badge thư viện

**Files:**
- Create: `apps/web/src/components/study/LocalVideoSource.tsx`
- Create: `apps/web/src/components/study/LocalVideoSource.test.tsx`
- Modify: `apps/web/src/components/study/StudyView.tsx` (khối video, state `localUrl`)
- Modify: `apps/web/src/lib/video.ts:25-34`
- Modify: `apps/web/src/components/library/LessonCard.tsx`
- Modify: `apps/web/src/components/ui/Icon.tsx` (thêm icon `video`, `folder-open`)

**Interfaces:**
- Produces: `<LocalVideoSource lesson onFile(file: File) />`; StudyView state `localUrl: string | null`; `<video src={videoUrl ?? localUrl}>`.

- [x] **Step 1: Icon** — thêm vào `PATHS`:

```tsx
  video: <><path d="m16 13 5.223 3.482a.5.5 0 0 0 .777-.416V7.87a.5.5 0 0 0-.752-.432L16 10.5" /><rect x="2" y="6" width="14" height="12" rx="2" /></>,
  "folder-open": <path d="m6 14 1.5-2.9A2 2 0 0 1 9.24 10H20a2 2 0 0 1 1.94 2.5l-1.54 6a2 2 0 0 1-1.95 1.5H4a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h3.9a2 2 0 0 1 1.69.9l.81 1.2a2 2 0 0 0 1.67.9H18a2 2 0 0 1 2 2v2" />,
```

- [x] **Step 2: Component test**

```tsx
// apps/web/src/components/study/LocalVideoSource.test.tsx
// @vitest-environment jsdom
import { describe, expect, it, vi } from "vitest";
import { fireEvent, render, screen } from "@testing-library/react";
import "fake-indexeddb/auto";
import { LocalVideoSource } from "./LocalVideoSource";

const lesson = { id: "l1", video_ref: "meeting.mp4", duration_sec: 125, video_size_bytes: null } as never;

describe("LocalVideoSource", () => {
  it("hiện gợi ý tên file + thời lượng và gọi onFile khi chọn (input fallback)", () => {
    const onFile = vi.fn();
    render(<LocalVideoSource lesson={lesson} onFile={onFile} />);
    expect(screen.getByText(/meeting\.mp4/)).toBeTruthy();
    expect(screen.getByText(/2:05/)).toBeTruthy();
    const input = screen.getByTestId("local-video-input") as HTMLInputElement;
    const file = new File(["x"], "meeting.mp4", { type: "video/mp4" });
    fireEvent.change(input, { target: { files: [file] } });
    expect(onFile).toHaveBeenCalledWith(file);
  });
});
```

- [x] **Step 3: Implement**

```tsx
// apps/web/src/components/study/LocalVideoSource.tsx
"use client";
import { useEffect, useState } from "react";
import type { LessonRow } from "@/lib/types";
import { formatDuration } from "@/lib/format";
import { forgetHandle, loadHandle, saveHandle, supportsFileHandles } from "@/lib/local-video";
import { Button } from "@/components/ui/Button";
import { Icon } from "@/components/ui/Icon";

type Props = { lesson: Pick<LessonRow, "id" | "video_ref" | "duration_sec" | "video_size_bytes">; onFile: (file: File) => void };

/** Khung "Chọn video trên máy" cho bài video_provider = 'local' (spec P2 §2.6, P2-2). */
export function LocalVideoSource({ lesson, onFile }: Props) {
  const [remembered, setRemembered] = useState<FileSystemFileHandle | null>(null);
  const [error, setError] = useState<string | null>(null);
  const canHandle = supportsFileHandles();

  useEffect(() => {
    if (!canHandle) return;
    loadHandle(lesson.id).then(setRemembered).catch(() => setRemembered(null));
  }, [lesson.id, canHandle]);

  const pickWithHandle = async () => {
    try {
      const [handle] = await window.showOpenFilePicker({ types: [{ description: "Video", accept: { "video/*": [".mp4", ".mov", ".m4v", ".webm"] } }], multiple: false });
      await saveHandle(lesson.id, handle);
      onFile(await handle.getFile());
    } catch (e) {
      if ((e as DOMException).name !== "AbortError") setError("Không mở được file.");
    }
  };
  const reopen = async () => {
    if (!remembered) return;
    try {
      const perm = await remembered.requestPermission({ mode: "read" });
      if (perm !== "granted") throw new Error("denied");
      onFile(await remembered.getFile());
    } catch {
      await forgetHandle(lesson.id);
      setRemembered(null);
      setError("File đã đổi chỗ hoặc bị từ chối — chọn lại.");
    }
  };

  const duration = formatDuration(lesson.duration_sec);
  return (
    <div className="flex aspect-video flex-col items-center justify-center gap-3 px-6 text-center text-sm text-[#A89684]">
      <Icon name="folder-open" className="size-9" />
      <p>Video của bài này nằm trên máy bạn, chưa lưu cloud.</p>
      {(lesson.video_ref || duration) && (
        <p className="text-xs">
          Gợi ý: {lesson.video_ref && <span className="font-mono">{lesson.video_ref}</span>}{lesson.video_ref && duration && " · "}{duration}
        </p>
      )}
      <div className="flex flex-wrap items-center justify-center gap-2">
        {remembered && <Button variant="primary" onClick={reopen} data-testid="local-video-reopen">Mở lại {remembered.name}</Button>}
        {canHandle ? (
          <Button variant={remembered ? "default" : "primary"} onClick={pickWithHandle}>
            <Icon name="video" className="size-[17px]" />Chọn video trên máy
          </Button>
        ) : (
          <label className="inline-flex min-h-[40px] cursor-pointer items-center gap-2 rounded-full border border-accent bg-accent px-4 text-sm font-semibold text-accent-fg">
            <Icon name="video" className="size-[17px]" />Chọn video trên máy
            <input
              data-testid="local-video-input" type="file" accept="video/*" className="sr-only"
              onChange={(e) => { const f = e.target.files?.[0]; if (f) onFile(f); e.target.value = ""; }}
            />
          </label>
        )}
      </div>
      {error && <p role="alert" className="text-xs text-danger">{error}</p>}
    </div>
  );
}
```
Kiểu `window.showOpenFilePicker`/`requestPermission`: thêm `apps/web/src/types/file-system-access.d.ts`:
```ts
interface Window {
  showOpenFilePicker(opts?: { multiple?: boolean; types?: { description?: string; accept: Record<string, string[]> }[] }): Promise<FileSystemFileHandle[]>;
}
interface FileSystemHandle { requestPermission(d?: { mode?: "read" | "readwrite" }): Promise<"granted" | "denied" | "prompt">; }
```

Run: `pnpm --filter web test -- src/components/study/LocalVideoSource.test.tsx` → PASS.

- [x] **Step 4: StudyView** — thêm state và banner:

```tsx
  const [localUrl, setLocalUrl] = useState<string | null>(null);
  const [localWarn, setLocalWarn] = useState<string[]>([]);
  const localFileRef = useRef<File | null>(null);
  const pickLocal = (file: File) => {
    if (localUrl) URL.revokeObjectURL(localUrl);
    localFileRef.current = file;
    setLocalWarn([]);
    setLocalUrl(URL.createObjectURL(file));
  };
  useEffect(() => () => { if (localUrl) URL.revokeObjectURL(localUrl); }, [localUrl]);
  const src = videoUrl ?? localUrl;
```
Khối video:
```tsx
        <div className="overflow-hidden rounded-md bg-video lg:rounded-lg">
          {src ? (
            <video
              ref={videoRef} src={src} controls playsInline className="aspect-video w-full"
              onLoadedMetadata={(e) => {
                if (!localFileRef.current) return;
                setLocalWarn(checkMatch(localFileRef.current, e.currentTarget.duration, lesson).reasons);
              }}
            />
          ) : lesson.video_provider === "local" ? (
            <LocalVideoSource lesson={lesson} onFile={pickLocal} />
          ) : (
            <div className="flex aspect-video flex-col items-center justify-center gap-2 text-sm text-[#A89684]">… (như cũ)</div>
          )}
        </div>
        {localWarn.length > 0 && (
          <div role="status" className="flex items-center justify-between gap-2 rounded-md bg-warning-soft px-3 py-2 text-xs text-warning">
            <span>File có thể không đúng ({localWarn.join(", ")}).</span>
            <button type="button" className="font-semibold underline" onClick={() => { setLocalUrl(null); localFileRef.current = null; setLocalWarn([]); }}>Chọn file khác</button>
          </div>
        )}
```
`useEffect` tạo `Html5PlayerAdapter` hiện phụ thuộc `[localCues]` và `videoRef.current` — thêm `src` vào deps để adapter được tạo lại khi `<video>` mount sau khi chọn file.

- [x] **Step 5: `video.ts`** — không đổi hàm (`getVideoUrl` đã trả null vì provider ≠ storage; `getThumbUrl` trả null vì `thumb_path` null). Thêm comment: `// local: không có URL/thumb — web chọn file phía client.`

- [x] **Step 6: LessonCard** — badge + icon:

```tsx
        {thumbUrl ? (<img … />) : (
          <div className="grid size-full place-items-center text-muted">
            {lesson.status === "error" ? <Icon name="alert-triangle" className="size-10 text-danger" />
              : lesson.video_provider === "local" ? <Icon name="video" className="size-10" />
              : <Icon name="video-off" className="size-10" />}
          </div>
        )}
        {lesson.video_provider === "local" && (
          <span className="absolute left-2 top-2 rounded-full bg-black/70 px-2 py-0.5 text-[11px] font-semibold text-white">Video local</span>
        )}
```

- [x] **Step 7: Kiểm tay** — tạo 1 bài local trong DB (Studio hoặc SQL: `update lessons set video_provider='local', video_ref='x.mp4', thumb_path=null where id=…` trên bản copy), mở `/lessons/<id>` trên dev server: thấy khung chọn file, chọn mp4 → phát và seek theo câu. `pnpm --filter web typecheck && pnpm --filter web test` xanh.

- [x] **Step 8: Commit**

```bash
git add apps/web/src
git commit -m "feat(web): play local-video lessons from a user-picked file; library badge"
```

---

## Khối B — Player abstraction & YouTube

### Task 6: `PlayerAdapter.onStateChange` + Html5

**Files:**
- Modify: `apps/web/src/lib/player.ts`
- Create: `apps/web/src/lib/player.test.ts`

**Interfaces:**
- Produces: `PlayerState = "playing" | "paused" | "ended"`; `PlayerAdapter.onStateChange(cb: (s: PlayerState) => void): () => void`.

- [x] **Step 1: Test**

```ts
// apps/web/src/lib/player.test.ts
// @vitest-environment jsdom
import { describe, expect, it, vi } from "vitest";
import { Html5PlayerAdapter } from "./player";

describe("Html5PlayerAdapter.onStateChange", () => {
  it("map play/pause/ended", () => {
    const v = document.createElement("video");
    const p = new Html5PlayerAdapter(v);
    const cb = vi.fn();
    const off = p.onStateChange(cb);
    v.dispatchEvent(new Event("play")); v.dispatchEvent(new Event("pause")); v.dispatchEvent(new Event("ended"));
    expect(cb.mock.calls.map((c) => c[0])).toEqual(["playing", "paused", "ended"]);
    off(); v.dispatchEvent(new Event("play"));
    expect(cb).toHaveBeenCalledTimes(3);
    p.destroy();
  });
});
```

- [x] **Step 2: Implement** — trong `player.ts`:

```ts
export type PlayerState = "playing" | "paused" | "ended";
export interface PlayerAdapter {
  play(): void; pause(): void; seekTo(ms: number): void; getCurrentTimeMs(): number; setRate(rate: number): void;
  onTime(cb: (ms: number) => void): () => void;
  onStateChange(cb: (s: PlayerState) => void): () => void;
  destroy(): void;
}
```
Html5: thêm `private stateListeners = new Set<(s: PlayerState) => void>();` và trong constructor:
```ts
    this.onPlay = () => this.stateListeners.forEach((cb) => cb("playing"));
    this.onPause = () => this.stateListeners.forEach((cb) => cb("paused"));
    this.onEnded = () => this.stateListeners.forEach((cb) => cb("ended"));
    video.addEventListener("play", this.onPlay); video.addEventListener("pause", this.onPause); video.addEventListener("ended", this.onEnded);
```
`onStateChange(cb) { this.stateListeners.add(cb); return () => this.stateListeners.delete(cb); }`; `destroy()` gỡ 3 listener và clear set.

Run: `pnpm --filter web test -- src/lib/player.test.ts` → PASS.

- [x] **Step 3: Commit** — `git commit -am "feat(web): player adapter exposes playback state"`

### Task 7: `YouTubePlayerAdapter`

**Files:**
- Create: `apps/web/src/lib/player-youtube.ts`
- Create: `apps/web/src/lib/player-youtube.test.ts`
- Create: `apps/web/src/types/youtube.d.ts`

**Interfaces:**
- Produces: `new YouTubePlayerAdapter(container: HTMLElement, videoId: string, opts: { onError: (code: number) => void; onReady?: () => void })`; `export function loadYouTubeApi(): Promise<typeof YT>`; static helper `interpolate(last: number, lastAt: number, now: number, rate: number): number` (export riêng để test).

- [x] **Step 1: Kiểu tối thiểu** `apps/web/src/types/youtube.d.ts`:

```ts
declare namespace YT {
  enum PlayerState { UNSTARTED = -1, ENDED = 0, PLAYING = 1, PAUSED = 2, BUFFERING = 3, CUED = 5 }
  interface Player {
    playVideo(): void; pauseVideo(): void; seekTo(s: number, allowSeekAhead: boolean): void;
    getCurrentTime(): number; setPlaybackRate(r: number): void; getPlaybackRate(): number; destroy(): void;
  }
  interface PlayerOptions {
    videoId: string; width?: string | number; height?: string | number;
    playerVars?: Record<string, string | number>;
    events?: { onReady?: (e: { target: Player }) => void; onStateChange?: (e: { data: number }) => void; onError?: (e: { data: number }) => void };
  }
  const Player: new (el: HTMLElement | string, opts: PlayerOptions) => Player;
}
interface Window { YT?: typeof YT; onYouTubeIframeAPIReady?: () => void }
```

- [x] **Step 2: Test**

```ts
// apps/web/src/lib/player-youtube.test.ts
// @vitest-environment jsdom
import { afterEach, beforeEach, describe, expect, it, vi } from "vitest";
import { YouTubePlayerAdapter, interpolate } from "./player-youtube";

type Events = NonNullable<YT.PlayerOptions["events"]>;
let events: Events; let fake: YT.Player & { t: number; rate: number };
beforeEach(() => {
  vi.useFakeTimers();
  fake = {
    t: 0, rate: 1,
    playVideo: vi.fn(), pauseVideo: vi.fn(), destroy: vi.fn(),
    seekTo: vi.fn((s: number) => { fake.t = s; }), getCurrentTime: () => fake.t,
    setPlaybackRate: vi.fn((r: number) => { fake.rate = r; }), getPlaybackRate: () => fake.rate,
  };
  window.YT = {
    PlayerState: { UNSTARTED: -1, ENDED: 0, PLAYING: 1, PAUSED: 2, BUFFERING: 3, CUED: 5 },
    Player: vi.fn((_el, opts: YT.PlayerOptions) => { events = opts.events!; queueMicrotask(() => events.onReady!({ target: fake })); return fake; }),
  } as unknown as typeof YT;
});
afterEach(() => { vi.useRealTimers(); delete window.YT; });

describe("interpolate", () => {
  it("nội suy theo rate", () => { expect(interpolate(10_000, 1000, 1500, 1.5)).toBe(10_750); });
});

describe("YouTubePlayerAdapter", () => {
  it("poll khi PLAYING, phát state, map error", async () => {
    const onError = vi.fn();
    const p = new YouTubePlayerAdapter(document.createElement("div"), "abc", { onError });
    await vi.advanceTimersByTimeAsync(0);
    const times: number[] = []; const states: string[] = [];
    p.onTime((ms) => times.push(ms)); p.onStateChange((s) => states.push(s));
    events.onStateChange!({ data: 1 }); fake.t = 4;
    await vi.advanceTimersByTimeAsync(260);
    expect(times.at(-1)).toBe(4000);
    events.onStateChange!({ data: 2 }); events.onStateChange!({ data: 0 });
    expect(states).toEqual(["playing", "paused", "ended"]);
    events.onError!({ data: 150 });
    expect(onError).toHaveBeenCalledWith(150);
    p.seekTo(12_500); expect(fake.seekTo).toHaveBeenCalledWith(12.5, true);
    p.setRate(1.25); expect(fake.setPlaybackRate).toHaveBeenCalledWith(1.25);
    p.destroy(); expect(fake.destroy).toHaveBeenCalled();
  });
  it("seek trước khi ready được xếp hàng và áp sau onReady", async () => {
    const p = new YouTubePlayerAdapter(document.createElement("div"), "abc", { onError: () => {} });
    p.seekTo(3000);
    expect(fake.seekTo).not.toHaveBeenCalled();
    await vi.advanceTimersByTimeAsync(0);
    expect(fake.seekTo).toHaveBeenCalledWith(3, true);
  });
});
```

- [x] **Step 3: Implement**

```ts
// apps/web/src/lib/player-youtube.ts — YouTube IFrame API adapter (spec P2 §3.2). Không autoplay.
import type { PlayerAdapter, PlayerState } from "./player";

const POLL_MS = 250;
let apiPromise: Promise<typeof YT> | null = null;

export function loadYouTubeApi(): Promise<typeof YT> {
  if (typeof window === "undefined") return Promise.reject(new Error("no window"));
  if (window.YT?.Player) return Promise.resolve(window.YT);
  if (!apiPromise) {
    apiPromise = new Promise((resolve) => {
      const prev = window.onYouTubeIframeAPIReady;
      window.onYouTubeIframeAPIReady = () => { prev?.(); resolve(window.YT!); };
      const s = document.createElement("script");
      s.src = "https://www.youtube.com/iframe_api"; s.async = true;
      document.head.appendChild(s);
    });
  }
  return apiPromise;
}

export function interpolate(lastMs: number, lastAt: number, now: number, rate: number): number {
  return Math.round(lastMs + (now - lastAt) * rate);
}

export class YouTubePlayerAdapter implements PlayerAdapter {
  private player: YT.Player | null = null;
  private ready = false;
  private pending: { seekMs?: number; rate?: number; play?: boolean } = {};
  private timeListeners = new Set<(ms: number) => void>();
  private stateListeners = new Set<(s: PlayerState) => void>();
  private poll = 0; private raf = 0;
  private lastMs = 0; private lastAt = 0; private rate = 1; private playing = false;

  constructor(container: HTMLElement, videoId: string, private opts: { onError: (code: number) => void; onReady?: () => void }) {
    loadYouTubeApi().then((yt) => {
      this.player = new yt.Player(container, {
        videoId, width: "100%", height: "100%",
        playerVars: { playsinline: 1, rel: 0, modestbranding: 1, autoplay: 0 },
        events: {
          onReady: () => {
            this.ready = true;
            if (this.pending.rate) this.player!.setPlaybackRate(this.pending.rate);
            if (this.pending.seekMs != null) this.player!.seekTo(this.pending.seekMs / 1000, true);
            if (this.pending.play) this.player!.playVideo();
            this.pending = {};
            this.opts.onReady?.();
          },
          onStateChange: (e) => this.handleState(e.data),
          onError: (e) => this.opts.onError(e.data),
        },
      });
    }).catch(() => this.opts.onError(-1));
  }

  private handleState(code: number) {
    const S = window.YT!.PlayerState;
    if (code === S.PLAYING) { this.playing = true; this.startPolling(); this.emit("playing"); }
    else if (code === S.PAUSED) { this.playing = false; this.stopPolling(); this.emit("paused"); }
    else if (code === S.ENDED) { this.playing = false; this.stopPolling(); this.emit("ended"); }
  }
  private emit(s: PlayerState) { this.stateListeners.forEach((cb) => cb(s)); }
  private sample() {
    if (!this.player) return;
    this.lastMs = Math.round(this.player.getCurrentTime() * 1000); this.lastAt = performance.now();
    this.rate = this.player.getPlaybackRate?.() ?? this.rate;
    this.timeListeners.forEach((cb) => cb(this.lastMs));
  }
  private startPolling() {
    this.stopPolling(); this.sample();
    this.poll = window.setInterval(() => this.sample(), POLL_MS);
    const tick = () => {
      if (!this.playing) return;
      const ms = interpolate(this.lastMs, this.lastAt, performance.now(), this.rate);
      this.timeListeners.forEach((cb) => cb(ms));
      this.raf = requestAnimationFrame(tick);
    };
    this.raf = requestAnimationFrame(tick);
  }
  private stopPolling() { window.clearInterval(this.poll); cancelAnimationFrame(this.raf); }

  play() { if (this.ready) this.player!.playVideo(); else this.pending.play = true; }
  pause() { this.player?.pauseVideo(); }
  seekTo(ms: number) { if (this.ready) this.player!.seekTo(ms / 1000, true); else this.pending.seekMs = ms; this.lastMs = ms; this.lastAt = performance.now(); }
  getCurrentTimeMs() { return this.ready ? Math.round(this.player!.getCurrentTime() * 1000) : this.lastMs; }
  setRate(rate: number) { this.rate = rate; if (this.ready) this.player!.setPlaybackRate(rate); else this.pending.rate = rate; }
  onTime(cb: (ms: number) => void) { this.timeListeners.add(cb); return () => this.timeListeners.delete(cb); }
  onStateChange(cb: (s: PlayerState) => void) { this.stateListeners.add(cb); return () => this.stateListeners.delete(cb); }
  destroy() { this.stopPolling(); this.timeListeners.clear(); this.stateListeners.clear(); this.player?.destroy(); this.player = null; }
}
```
Lưu ý `seekTo` trên player đã ready nhưng chưa từng play: YouTube sẽ tự phát — với hash-seek/seek ban đầu (Task 12) StudyView chỉ gọi `seekTo` khi state đã `playing` lần đầu (giữ `pendingSeek` ở StudyView cho provider youtube), xem Task 8 Step 3.

Run: `pnpm --filter web test -- src/lib/player-youtube.test.ts` → PASS.

- [x] **Step 4: Commit** — `git add apps/web/src && git commit -m "feat(web): YouTube IFrame player adapter with polling and interpolation"`

### Task 8: `VideoFrame` + StudyView dùng adapter theo provider

**Files:**
- Create: `apps/web/src/components/study/VideoFrame.tsx`
- Create: `apps/web/src/components/study/VideoFrame.test.tsx`
- Modify: `apps/web/src/components/study/StudyView.tsx` (bỏ `videoRef`, khối video, effect adapter)

**Interfaces:**
- Produces:
  ```tsx
  export type VideoFrameProps = {
    lesson: LessonRow; videoUrl: string | null; localUrl: string | null; mode: "full" | "compact";
    onReady: (player: PlayerAdapter) => void; onLocalFile: (f: File) => void;
    onLoadedMetadata?: (durationSec: number) => void; onError?: (msg: string) => void; badge: { kind: BadgeKind; label: string } | null;
  };
  ```
  `mode` chưa dùng ở task này (luôn `"full"`), Task 13 dùng.

- [x] **Step 1: Test**

```tsx
// apps/web/src/components/study/VideoFrame.test.tsx
// @vitest-environment jsdom
import { describe, expect, it, vi } from "vitest";
import { render, screen } from "@testing-library/react";
import "fake-indexeddb/auto";
import { VideoFrame } from "./VideoFrame";

const base = { id: "l1", title: "t", video_ref: null, duration_sec: null, video_size_bytes: null, status: "ready" } as never;
const common = { mode: "full" as const, onReady: vi.fn(), onLocalFile: vi.fn(), badge: null, localUrl: null };

describe("VideoFrame", () => {
  it("storage → <video> + adapter ready", () => {
    render(<VideoFrame {...common} lesson={{ ...base, video_provider: "storage" }} videoUrl="blob:x" />);
    expect(document.querySelector("video")?.getAttribute("src")).toBe("blob:x");
    expect(common.onReady).toHaveBeenCalled();
  });
  it("local chưa có file → khung chọn file", () => {
    render(<VideoFrame {...common} lesson={{ ...base, video_provider: "local" }} videoUrl={null} />);
    expect(screen.getByText(/Chọn video trên máy/)).toBeTruthy();
  });
  it("youtube → container iframe", () => {
    render(<VideoFrame {...common} lesson={{ ...base, video_provider: "youtube", video_ref: "abc" }} videoUrl={null} />);
    expect(screen.getByTestId("youtube-frame")).toBeTruthy();
  });
  it("không có nguồn → Video chưa sẵn sàng", () => {
    render(<VideoFrame {...common} lesson={{ ...base, video_provider: "storage", status: "draft" }} videoUrl={null} />);
    expect(screen.getByText(/Video chưa sẵn sàng/)).toBeTruthy();
  });
});
```

- [x] **Step 2: Implement**

```tsx
// apps/web/src/components/study/VideoFrame.tsx — khung video theo provider (spec P2 §3.3). Trả PlayerAdapter qua onReady.
"use client";
import { useEffect, useRef, useState } from "react";
import type { LessonRow } from "@/lib/types";
import type { BadgeKind } from "@/lib/lesson-status";
import { Html5PlayerAdapter, type PlayerAdapter } from "@/lib/player";
import { YouTubePlayerAdapter } from "@/lib/player-youtube";
import { cn } from "@/lib/cn";
import { Icon } from "@/components/ui/Icon";
import { Badge } from "@/components/ui/Badge";
import { LocalVideoSource } from "./LocalVideoSource";

export type VideoFrameProps = {
  lesson: LessonRow; videoUrl: string | null; localUrl: string | null; mode: "full" | "compact";
  onReady: (player: PlayerAdapter) => void; onLocalFile: (f: File) => void;
  onLoadedMetadata?: (durationSec: number) => void; onError?: (msg: string) => void;
  badge: { kind: BadgeKind; label: string } | null;
};

const YT_ERRORS: Record<number, string> = {
  2: "Mã video không hợp lệ.", 5: "Trình duyệt không phát được video này.",
  100: "Video đã bị xoá hoặc ở chế độ riêng tư.", 101: "Chủ video không cho nhúng.", 150: "Chủ video không cho nhúng.",
};

export function VideoFrame({ lesson, videoUrl, localUrl, mode, onReady, onLocalFile, onLoadedMetadata, onError, badge }: VideoFrameProps) {
  const videoRef = useRef<HTMLVideoElement>(null);
  const ytRef = useRef<HTMLDivElement>(null);
  const [ytError, setYtError] = useState<string | null>(null);
  const src = videoUrl ?? localUrl;
  const isYoutube = lesson.video_provider === "youtube" && !!lesson.video_ref;

  useEffect(() => {
    let player: PlayerAdapter | null = null;
    if (isYoutube && ytRef.current) {
      player = new YouTubePlayerAdapter(ytRef.current, lesson.video_ref!, {
        onError: (code) => { const m = YT_ERRORS[code] ?? "Không phát được video."; setYtError(m); onError?.(m); },
      });
    } else if (src && videoRef.current) {
      player = new Html5PlayerAdapter(videoRef.current);
    }
    if (player) onReady(player);
    return () => player?.destroy();
    // eslint-disable-next-line react-hooks/exhaustive-deps
  }, [isYoutube, lesson.video_ref, src]);

  const frame = cn("overflow-hidden bg-video transition-[width,height] duration-200", mode === "compact" ? "rounded-sm" : "rounded-md lg:rounded-lg");
  if (isYoutube) {
    return (
      <div className={frame}>
        <div className="relative aspect-video w-full">
          <div ref={ytRef} data-testid="youtube-frame" className="absolute inset-0 [&>iframe]:size-full" />
          {ytError && (
            <div className="absolute inset-0 flex flex-col items-center justify-center gap-2 bg-video px-6 text-center text-sm text-[#A89684]">
              <Icon name="alert-triangle" className="size-8" /><span>{ytError}</span>
              <a className="underline" href={`https://www.youtube.com/watch?v=${lesson.video_ref}`} target="_blank" rel="noreferrer">Mở trên YouTube</a>
            </div>
          )}
        </div>
      </div>
    );
  }
  return (
    <div className={frame}>
      {src ? (
        <video ref={videoRef} src={src} controls playsInline className="aspect-video w-full"
          onLoadedMetadata={(e) => onLoadedMetadata?.(e.currentTarget.duration)} />
      ) : lesson.video_provider === "local" ? (
        <LocalVideoSource lesson={lesson} onFile={onLocalFile} />
      ) : (
        <div className="flex aspect-video flex-col items-center justify-center gap-2 text-sm text-[#A89684]">
          <Icon name="video-off" className="size-9" /><span>Video chưa sẵn sàng</span>
          {badge && <Badge kind={badge.kind}>{badge.label}</Badge>}
        </div>
      )}
    </div>
  );
}
```
Kiểm tra `bg-video`/`.bg-video` token có trong `globals.css` (`--video`) — đã có (dùng ở StudyView).

- [x] **Step 3: StudyView** — thay khối video bằng `<VideoFrame … />`, bỏ `videoRef`, đổi effect adapter:

```tsx
  const playerRef = useRef<PlayerAdapter | null>(null);
  const pendingSeekRef = useRef<number | null>(null);   // youtube: chờ PLAYING đầu tiên (spec §3.2)
  const [playerState, setPlayerState] = useState<PlayerState>("paused");
  const onPlayerReady = (player: PlayerAdapter) => {
    playerRef.current = player;
    player.onTime((ms) => {
      setActiveIdx(findActiveCueIndex(localCuesRef.current, ms));
      const ab = abRef.current;
      if (ab && ms >= ab.end) player.seekTo(ab.start);
    });
    player.onStateChange((s) => {
      setPlayerState(s);
      if (s === "playing" && pendingSeekRef.current != null) { player.seekTo(pendingSeekRef.current); pendingSeekRef.current = null; }
    });
    const initial = initialSeekMs();
    if (initial != null) { if (lesson.video_provider === "youtube") pendingSeekRef.current = initial; else player.seekTo(initial); }
  };
```
`localCuesRef` = `useRef(localCues)` cập nhật mỗi render (thay cho deps `[localCues]` cũ). `initialSeekMs()` đọc hash `#cue=`/`#cueid=` như hiện tại (đến Task 12 sẽ thêm câu chưa học đầu). Xoá `useEffect` cũ tạo `Html5PlayerAdapter`. Banner `localWarn` giữ từ Task 5, nhận `durationSec` từ `onLoadedMetadata`.

Run: `pnpm --filter web typecheck && pnpm --filter web test && pnpm e2e` (smoke) → PASS. Kiểm tay: bài storage phát/seek như cũ; sửa tạm 1 bài thành youtube (`video_provider='youtube', video_ref='<id>'`) → iframe, bấm play, câu highlight theo, A–B, tốc độ; đổi `video_ref` thành id không cho nhúng → thông báo lỗi.

- [x] **Step 4: Commit** — `git add apps/web/src && git commit -m "feat(web): VideoFrame picks player adapter per provider; YouTube lessons play in study view"`

---

## Khối C — Share community

### Task 9: `Dialog` dùng chung

**Files:**
- Create: `apps/web/src/components/ui/Dialog.tsx`
- Create: `apps/web/src/components/ui/Dialog.test.tsx`

**Interfaces:**
- Produces: `<Dialog open onClose title children footer? testId? />` — native `<dialog>`, `showModal()` khi `open`, đóng khi Esc/backdrop.

- [x] **Step 1: Test**

```tsx
// @vitest-environment jsdom
import { describe, expect, it, vi } from "vitest";
import { fireEvent, render, screen } from "@testing-library/react";
import { Dialog } from "./Dialog";

describe("Dialog", () => {
  it("mở bằng showModal, đóng khi cancel", () => {
    HTMLDialogElement.prototype.showModal = vi.fn(function (this: HTMLDialogElement) { this.setAttribute("open", ""); });
    HTMLDialogElement.prototype.close = vi.fn(function (this: HTMLDialogElement) { this.removeAttribute("open"); });
    const onClose = vi.fn();
    render(<Dialog open onClose={onClose} title="Tiêu đề"><p>nội dung</p></Dialog>);
    const dlg = screen.getByRole("dialog");
    expect(HTMLDialogElement.prototype.showModal).toHaveBeenCalled();
    expect(screen.getByText("Tiêu đề")).toBeTruthy();
    fireEvent(dlg, new Event("cancel"));
    expect(onClose).toHaveBeenCalled();
  });
});
```

- [x] **Step 2: Implement**

```tsx
// apps/web/src/components/ui/Dialog.tsx — native <dialog>, token theme, dùng chung (spec P2 §4.2).
"use client";
import { useEffect, useId, useRef, type ReactNode } from "react";
import { cn } from "@/lib/cn";

export function Dialog({ open, onClose, title, children, footer, className, testId }: {
  open: boolean; onClose: () => void; title: string; children: ReactNode; footer?: ReactNode; className?: string; testId?: string;
}) {
  const ref = useRef<HTMLDialogElement>(null);
  const titleId = useId();
  useEffect(() => {
    const d = ref.current; if (!d) return;
    if (open && !d.open) d.showModal();
    if (!open && d.open) d.close();
  }, [open]);
  return (
    <dialog
      ref={ref} data-testid={testId} aria-labelledby={titleId}
      onCancel={(e) => { e.preventDefault(); onClose(); }}
      onClick={(e) => { if (e.target === e.currentTarget) onClose(); }}
      className={cn("m-auto w-[min(92vw,440px)] rounded-lg border border-line bg-surface p-0 text-ink shadow-card backdrop:bg-black/40", className)}
    >
      <div className="flex flex-col gap-3 p-5">
        <h2 id={titleId} className="text-[17px] font-bold">{title}</h2>
        <div className="text-sm text-muted">{children}</div>
        {footer && <div className="mt-1 flex flex-wrap justify-end gap-2">{footer}</div>}
      </div>
    </dialog>
  );
}
```

Run: `pnpm --filter web test -- src/components/ui/Dialog.test.tsx` → PASS. Commit: `git commit -m "feat(web): shared Dialog primitive on native <dialog>"`.

### Task 10: Nút Chia sẻ + badge thư viện + pgTAP + e2e

**Files:**
- Create: `apps/web/src/components/study/ShareButton.tsx`
- Create: `apps/web/src/components/study/ShareButton.test.tsx`
- Modify: `apps/web/src/components/study/StudyView.tsx` (hàng tiêu đề mobile + `StudyControls` desktop)
- Modify: `apps/web/src/components/study/StudyControls.tsx` (slot `extra?: ReactNode` cạnh nút Sửa)
- Modify: `apps/web/src/components/library/LessonCard.tsx`, `apps/web/src/app/(app)/page.tsx`
- Modify: `apps/web/src/components/ui/Icon.tsx` (icon `share`)
- Modify: `supabase/tests/001_rls.sql` (thêm 2 case → `plan(10)`)
- Create: `e2e/share.spec.ts`

**Interfaces:**
- Produces: `<ShareButton lesson visibility onChange(v) />` với `visibility: "private" | "community"`; `LessonCard` nhận thêm `ownerName?: string | null`, `isMine: boolean`.

- [x] **Step 1: Icon** — `share: <><circle cx="18" cy="5" r="3" /><circle cx="6" cy="12" r="3" /><circle cx="18" cy="19" r="3" /><path d="m8.59 13.51 6.83 3.98" /><path d="m15.41 6.51-6.82 3.98" /></>,`

- [x] **Step 2: pgTAP** — thêm vào `001_rls.sql` (đang đóng vai user khác), `plan(10)`:

```sql
select is((select count(*) from public.lessons where id='bbbb0000-0000-4000-8000-00000000000b' and visibility='community'), 1::bigint, 'đọc được visibility bài community');
update public.lessons set visibility='private' where id='bbbb0000-0000-4000-8000-00000000000b';
select is((select visibility from public.lessons where id='bbbb0000-0000-4000-8000-00000000000b'), 'community', 'người khác không đổi được visibility (UPDATE 0 dòng)');
```
Run: `supabase test db` → PASS.

- [x] **Step 3: ShareButton test**

```tsx
// @vitest-environment jsdom
import { describe, expect, it, vi } from "vitest";
import { fireEvent, render, screen } from "@testing-library/react";
import { ShareButton } from "./ShareButton";

HTMLDialogElement.prototype.showModal = function () { this.setAttribute("open", ""); };
HTMLDialogElement.prototype.close = function () { this.removeAttribute("open"); };
const zoom = { source_type: "zoom", status: "ready", video_provider: "storage" } as never;

describe("ShareButton", () => {
  it("zoom: cần tick xác nhận mới bật Chia sẻ", () => {
    const onChange = vi.fn();
    render(<ShareButton lesson={zoom} visibility="private" onChange={onChange} />);
    fireEvent.click(screen.getByRole("button", { name: "Chia sẻ" }));
    const confirm = screen.getByRole("button", { name: /Chia sẻ lên cộng đồng/ });
    expect(confirm).toBeDisabled();
    fireEvent.click(screen.getByRole("checkbox", { name: /Tôi đã xác nhận/ }));
    fireEvent.click(confirm);
    expect(onChange).toHaveBeenCalledWith("community");
  });
  it("đang community → bấm là ngừng chia sẻ, không dialog", () => {
    const onChange = vi.fn();
    render(<ShareButton lesson={zoom} visibility="community" onChange={onChange} />);
    fireEvent.click(screen.getByRole("button", { name: "Đang chia sẻ" }));
    expect(onChange).toHaveBeenCalledWith("private");
  });
  it("bài chưa ready: disabled", () => {
    render(<ShareButton lesson={{ ...zoom, status: "draft" }} visibility="private" onChange={vi.fn()} />);
    expect(screen.getByRole("button", { name: "Chia sẻ" })).toBeDisabled();
  });
});
```

- [x] **Step 4: Implement ShareButton**

```tsx
// apps/web/src/components/study/ShareButton.tsx — đổi visibility bài (owner). Dialog xác nhận riêng cho zoom (spec P2 §4.2, P2-7).
"use client";
import { useState } from "react";
import type { LessonRow } from "@/lib/types";
import { IconButton } from "@/components/ui/IconButton";
import { Button } from "@/components/ui/Button";
import { Checkbox } from "@/components/ui/Checkbox";
import { Dialog } from "@/components/ui/Dialog";
import { Icon } from "@/components/ui/Icon";

export function ShareButton({ lesson, visibility, onChange }: {
  lesson: Pick<LessonRow, "source_type" | "status" | "video_provider">;
  visibility: LessonRow["visibility"]; onChange: (v: LessonRow["visibility"]) => void;
}) {
  const [open, setOpen] = useState(false);
  const [confirmed, setConfirmed] = useState(false);
  const shared = visibility === "community";
  const isZoom = lesson.source_type === "zoom";
  const notReady = lesson.status !== "ready";
  const click = () => { if (shared) onChange("private"); else setOpen(true); };
  const doShare = () => { onChange("community"); setOpen(false); setConfirmed(false); };
  return (
    <>
      <IconButton
        label={shared ? "Đang chia sẻ" : "Chia sẻ"} pressed={shared} onClick={click} disabled={notReady}
        title={notReady ? "Chỉ chia sẻ được bài đã sẵn sàng" : undefined} data-testid="share-button"
      >
        <Icon name="share" className="size-[18px]" />
      </IconButton>
      <Dialog open={open} onClose={() => setOpen(false)} title={isZoom ? "Chia sẻ bản ghi họp?" : "Chia sẻ lên cộng đồng?"} testId="share-dialog"
        footer={<>
          <Button onClick={() => setOpen(false)}>Huỷ</Button>
          <Button variant="primary" onClick={doShare} disabled={isZoom && !confirmed}>Chia sẻ lên cộng đồng</Button>
        </>}
      >
        <p>Mọi người dùng Vingo sẽ xem được bài, transcript và từ vựng. Tiến độ học vẫn riêng từng người.</p>
        {isZoom && <p className="mt-2">Người trong video đã đồng ý chưa? Tên khách hàng, dự án nhạy cảm đã ẩn chưa?</p>}
        {lesson.video_provider === "local" && <p className="mt-2">Video nằm trên máy bạn — người xem sẽ phải tự có file video.</p>}
        {isZoom && (
          <label className="mt-3 flex items-center gap-2 text-ink">
            <Checkbox checked={confirmed} onChange={() => setConfirmed((c) => !c)} label="Tôi đã xác nhận" />
            Tôi đã xác nhận
          </label>
        )}
      </Dialog>
    </>
  );
}
```

- [x] **Step 5: StudyView** — state + mutation:

```tsx
  const [visibility, setVisibility] = useState(lesson.visibility);
  const changeVisibility = async (v: LessonRow["visibility"]) => {
    const prev = visibility; setVisibility(v);
    const { error } = await supabase.from("lessons").update({ visibility: v }).eq("id", lesson.id);
    if (error) { setVisibility(prev); notify(`Lỗi: ${error.message}`); }
    else notify(v === "community" ? "Đã chia sẻ lên cộng đồng" : "Đã ngừng chia sẻ");
  };
```
Hàng tiêu đề mobile: `{canEdit && <ShareButton lesson={lesson} visibility={visibility} onChange={changeVisibility} />}` trước nút Sửa. Desktop: `StudyControls` thêm prop `extra?: ReactNode` render trước nút Sửa (`{extra}`); StudyView truyền `extra={canEdit && <ShareButton …/>}`.

- [x] **Step 6: Thư viện** — `page.tsx` sau khi có `lessons`:

```ts
  const ownerIds = [...new Set((lessons ?? []).map((l: LessonRow) => l.owner_id))];
  const { data: profiles } = ownerIds.length
    ? await supabase.from("profiles").select("id, display_name").in("id", ownerIds)
    : { data: [] as { id: string; display_name: string }[] };
  const nameOf = new Map((profiles ?? []).map((p) => [p.id, p.display_name]));
```
Truyền `ownerName={tab === "community" ? nameOf.get(l.owner_id) ?? null : null}` và `isMine={l.owner_id === user.id}`. `LessonCard`:
```tsx
        {lesson.visibility === "community" && isMine && <Badge kind="draft">{ownerName != null ? "Của bạn" : "Cộng đồng"}</Badge>}
        {ownerName && !isMine && <p className="text-xs text-muted">bởi {ownerName}</p>}
```
(đặt cạnh dòng ngày).

- [x] **Step 7: e2e** `e2e/share.spec.ts` (login helper copy từ `smoke.spec.ts`):

```ts
test("share → tab Cộng đồng thấy bài → ngừng share", async ({ page }) => {
  await login(page);
  await page.locator("a[href^='/lessons/']").first().click();
  const url = page.url();
  const btn = page.getByTestId("share-button");
  const wasShared = (await btn.getAttribute("aria-pressed")) === "true";
  if (wasShared) { await btn.click(); await expect(btn).toHaveAttribute("aria-pressed", "false"); }
  await btn.click();
  const dlg = page.getByTestId("share-dialog");
  const cb = dlg.getByRole("checkbox");
  if (await cb.count()) await cb.click();
  await dlg.getByRole("button", { name: /Chia sẻ lên cộng đồng/ }).click();
  await expect(btn).toHaveAttribute("aria-pressed", "true");
  await page.goto("/?tab=community");
  await expect(page.locator(`a[href='${new URL(url).pathname}']`)).toBeVisible();
  await page.goto(url);
  await page.getByTestId("share-button").click();
  await expect(page.getByTestId("share-button")).toHaveAttribute("aria-pressed", "false");
  if (wasShared) { /* trả lại trạng thái ban đầu */ await page.getByTestId("share-button").click(); const d = page.getByTestId("share-dialog"); const c = d.getByRole("checkbox"); if (await c.count()) await c.click(); await d.getByRole("button", { name: /Chia sẻ lên cộng đồng/ }).click(); }
});
```

Run: `pnpm --filter web test && pnpm --filter web typecheck && pnpm e2e` → PASS.

- [x] **Step 8: Commit** — `git add -A apps/web/src supabase/tests e2e/share.spec.ts && git commit -m "feat(web): share lessons to community with zoom consent dialog; owner names in library"`

---

## Khối D — Polish mobile

### Task 11: M3 Settings tràn ngang + M4 nút CSV từ điển

**Files:**
- Modify: `apps/web/src/components/settings/SettingsView.tsx:136-141`
- Modify: `apps/web/src/components/dictionary/DictionaryTable.tsx:96-106`

- [x] **Step 1: Settings** — ô lệnh:

```tsx
            <div key={cmd} className="flex items-center gap-2 rounded-lg border border-line bg-surface-2 py-2 pl-3 pr-2">
              <pre className="min-w-0 flex-1 overflow-x-auto whitespace-pre font-mono text-[13px] leading-relaxed [scrollbar-width:thin]">{cmd}</pre>
              <CopyButton value={cmd} />
            </div>
```
(`<pre>` thay `<code>`: `overflow-x-auto` cần block; `min-w-0` để flex cho phép co.)

- [x] **Step 2: Dictionary toolbar** — hàng filter:

```tsx
        <div className="flex flex-wrap items-center gap-2">
          <SegmentedControl ariaLabel="Lọc theo trạng thái" tight value={filter} onChange={setFilter} items={FILTERS} />
          <span className="ml-auto inline-flex items-center gap-2">
            {countPill}
            <span className="md:hidden"><IconButton label="Tải CSV" onClick={exportCsv}><Icon name="download" className="size-[18px]" /></IconButton></span>
            <span className="hidden md:inline-flex"><Button onClick={exportCsv}><Icon name="download" className="size-[17px]" />CSV</Button></span>
          </span>
        </div>
```
Xoá 2 chỗ `countPill` cũ. `DictionaryTable.test.tsx` nếu assert vị trí pill thì cập nhật.

- [x] **Step 3: Kiểm** — `pnpm --filter web test`; mở `/settings` và `/dictionary` ở 375px: lệnh cuộn ngang trong ô, CSV cùng hàng với filter. Commit: `git commit -am "fix(web): settings command overflow and dictionary toolbar on narrow screens"`.

### Task 12: M6 nút câu trước/sau + seek ban đầu, M5 Wake Lock

**Files:**
- Create: `apps/web/src/lib/use-wake-lock.ts`, `apps/web/src/lib/use-wake-lock.test.tsx`
- Modify: `apps/web/src/lib/cues.ts` (+ `cues.test.ts`): `stepIndex`
- Modify: `apps/web/src/components/study/StudyView.tsx` (hàng tab mobile, `initialSeekMs`)
- Modify: `apps/web/src/components/ui/Icon.tsx` (`chevron-right`)

**Interfaces:**
- Produces: `stepIndex(activeIdx: number, delta: 1 | -1, total: number, fallback: number): number`; `useWakeLock(active: boolean): void`.

- [x] **Step 1: `stepIndex` test + impl** trong `cues.ts`:

```ts
/** Chỉ số câu khi bấm ‹ › : chưa có câu đang phát → fallback (câu chưa học đầu, hoặc 0). Kẹp trong [0, total-1]. */
export function stepIndex(activeIdx: number, delta: 1 | -1, total: number, fallback: number): number {
  if (total <= 0) return -1;
  if (activeIdx < 0) return Math.max(0, Math.min(total - 1, fallback < 0 ? 0 : fallback));
  return Math.max(0, Math.min(total - 1, activeIdx + delta));
}
```
Test: `stepIndex(-1, 1, 5, 2) === 2`, `stepIndex(-1, 1, 5, -1) === 0`, `stepIndex(4, 1, 5, 0) === 4`, `stepIndex(0, -1, 5, 0) === 0`, `stepIndex(2, 1, 5, 0) === 3`, `stepIndex(0, 1, 0, 0) === -1`.

- [x] **Step 2: Wake Lock hook test**

```tsx
// @vitest-environment jsdom
import { describe, expect, it, vi } from "vitest";
import { renderHook } from "@testing-library/react";
import { useWakeLock } from "./use-wake-lock";

describe("useWakeLock", () => {
  it("request khi active, release khi tắt; không có API thì bỏ qua", async () => {
    const release = vi.fn().mockResolvedValue(undefined);
    const request = vi.fn().mockResolvedValue({ release, released: false });
    Object.defineProperty(navigator, "wakeLock", { value: { request }, configurable: true });
    const { rerender, unmount } = renderHook(({ on }) => useWakeLock(on), { initialProps: { on: true } });
    await Promise.resolve();
    expect(request).toHaveBeenCalledWith("screen");
    rerender({ on: false }); await Promise.resolve();
    expect(release).toHaveBeenCalled();
    unmount();
    Object.defineProperty(navigator, "wakeLock", { value: undefined, configurable: true });
    expect(() => renderHook(() => useWakeLock(true))).not.toThrow();
  });
});
```

- [x] **Step 3: Hook impl**

```ts
// apps/web/src/lib/use-wake-lock.ts — giữ màn hình sáng khi đang phát (spec P2 §6 M5).
"use client";
import { useEffect } from "react";

export function useWakeLock(active: boolean): void {
  useEffect(() => {
    if (!active || typeof navigator === "undefined" || !("wakeLock" in navigator)) return;
    let sentinel: WakeLockSentinel | null = null;
    let cancelled = false;
    const acquire = async () => {
      try { sentinel = await navigator.wakeLock.request("screen"); if (cancelled) await sentinel.release(); } catch { /* bị từ chối (pin yếu, tab ẩn) → bỏ qua */ }
    };
    const onVis = () => { if (document.visibilityState === "visible" && (!sentinel || sentinel.released)) void acquire(); };
    void acquire();
    document.addEventListener("visibilitychange", onVis);
    return () => { cancelled = true; document.removeEventListener("visibilitychange", onVis); void sentinel?.release(); };
  }, [active]);
}
```
StudyView: `useWakeLock(playerState === "playing")`.

- [x] **Step 4: Nút ‹ › + seek ban đầu** — StudyView hàng tab mobile:

```tsx
        <div className="flex items-center justify-between gap-2 lg:hidden">
          <SegmentedControl … />
          <div className="flex items-center gap-1">
            <IconButton label="Câu trước" onClick={() => seekToCue(stepIndex(activeIdx, -1, localCues.length, nextIdx))} disabled={!localCues.length} data-testid="prev-cue"><Icon name="chevron-left" className="size-5" /></IconButton>
            <IconButton label="Câu sau" onClick={() => seekToCue(stepIndex(activeIdx, 1, localCues.length, nextIdx))} disabled={!localCues.length} data-testid="next-cue"><Icon name="chevron-right" className="size-5" /></IconButton>
            <ProgressRing value={percent(doneIds.size, localCues.length)} size={32} label />
          </div>
        </div>
```
Icon `"chevron-right": <path d="m9 18 6-6-6-6" />`. `initialSeekMs()`: sau khi kiểm hash, nếu không có hash → `const i = nextUndoneIndex(localCues, doneIds); return i > 0 ? localCues[i].start_ms : null;` (i = 0 hoặc -1 → không seek). `seekToCue` giữ `play()` như cũ (bấm ‹ › là cử chỉ người dùng nên YouTube cho phép).

Run: `pnpm --filter web test && pnpm --filter web typecheck`. Kiểm tay: mở bài đã học vài câu → video đứng ở câu chưa học đầu; ‹ › nhảy câu. Commit: `git commit -am "feat(web): prev/next cue buttons, resume at first unlearned cue, screen wake lock while playing"`.

### Task 13: M2 video gọn khi cuộn + M1 landscape + M7 safe-area + e2e mobile

**Files:**
- Modify: `apps/web/src/app/globals.css` (variant `land`)
- Modify: `apps/web/src/components/study/StudyView.tsx` (layout, `mode`)
- Modify: `apps/web/src/components/study/VideoFrame.tsx` (class theo `mode`)
- Modify: `apps/web/src/components/study/MobileDock.tsx`, `apps/web/src/components/layout/AppHeader.tsx`
- Modify: `apps/web/src/app/layout.tsx` (`viewport`), `apps/web/src/app/manifest.ts`
- Create: `e2e/mobile.spec.ts`

- [x] **Step 1: Variant `land`** — đầu `globals.css` sau `@import "tailwindcss";`:

```css
/* Điện thoại xoay ngang: trang học 2 cột, ẩn header/dock (spec P2 §6 M1) */
@custom-variant land (@media (orientation: landscape) and (max-height: 500px));
```

- [x] **Step 2: Compact mode** — StudyView:

```tsx
  const [videoMode, setVideoMode] = useState<"full" | "compact">("full");
  useEffect(() => {
    const onScroll = () => setVideoMode(window.scrollY > 120 ? "compact" : "full");
    window.addEventListener("scroll", onScroll, { passive: true });
    return () => window.removeEventListener("scroll", onScroll);
  }, []);
```
Khối sticky trái (mobile) đổi thành:
```tsx
      <div className="sticky top-0 z-10 -mx-4 flex flex-col gap-2 bg-page px-4 pb-2 sm:-mx-6 sm:px-6 lg:static lg:mx-0 lg:gap-3 lg:px-0 lg:pb-0 land:static land:mx-0 land:h-[100dvh] land:overflow-hidden land:px-0 land:pb-0">
        <div className={cn("flex h-[52px] items-center gap-1 lg:hidden", videoMode === "compact" && "land:flex")}> … tiêu đề … 
          {/* compact: video thu nhỏ nằm cuối hàng tiêu đề */}
          {videoMode === "compact" && (
            <button type="button" aria-label="Mở rộng video" onClick={() => { window.scrollTo({ top: 0, behavior: "smooth" }); }} className="ml-1 w-[170px] shrink-0 lg:hidden land:hidden" data-testid="video-compact">
              <VideoFrame … mode="compact" />
            </button>
          )}
        </div>
        <div className={cn(videoMode === "compact" && "hidden lg:block land:block")} data-testid="video-full">
          <VideoFrame … mode="full" />
        </div>
```
**Quan trọng:** không mount 2 `VideoFrame` cùng lúc (player bị tạo lại, iframe reload). Cách làm: giữ **một** `VideoFrame` duy nhất, đổi vị trí bằng CSS thay vì remount — bọc trong `div` có class theo mode:
```tsx
        <div className={cn(
          "transition-[width] duration-200",
          videoMode === "compact" ? "absolute right-4 top-[6px] z-20 w-[170px] sm:right-6 lg:static lg:w-auto land:static land:w-auto" : "w-full",
        )} data-testid="video-frame" data-mode={videoMode}>
          <VideoFrame … mode={videoMode} />
        </div>
```
Khối sticky thêm `relative`; khi compact, hàng tiêu đề chừa chỗ (`pr-[180px]`). Chạm video compact → `window.scrollTo({ top: 0 })` (gắn `onClick` lên wrapper, chỉ khi compact; video vẫn nhận controls vì click vào controls dừng propagation không cần — `scrollTo` không ảnh hưởng phát).

- [x] **Step 3: Landscape** — `<main>`: thêm `land:grid land:h-[100dvh] land:grid-cols-[55%_45%] land:gap-3 land:pb-0`; khối phải thêm `land:h-[100dvh] land:overflow-y-auto land:pt-0`; `StudyControls className="hidden lg:flex land:flex"` (dạng dải mỏng: thêm prop `dense` bỏ dòng hint và padding); `MobileDock` root thêm `land:hidden`; `AppHeader` `<header>` thêm `land:hidden`; layout `(app)` padding `land:py-0`. `CueItem` `scroll-mt-[330px]` đổi `land:scroll-mt-2`.

- [x] **Step 4: Safe-area & manifest** — `layout.tsx`:
```ts
export const viewport: Viewport = { viewportFit: "cover", themeColor: [ …giữ… ] };
```
`AppHeader` `<header className="border-b border-line bg-page pt-[env(safe-area-inset-top)] land:hidden">`. Khối sticky trang học `top-0` → `top-[env(safe-area-inset-top)]`. `manifest.ts` thêm `id: "/"`, `orientation: "any"`.

- [x] **Step 5: e2e** `e2e/mobile.spec.ts`:

```ts
import { devices, expect, test, type Page } from "@playwright/test";
test.use({ ...devices["iPhone 13"], defaultBrowserType: undefined });
// login() như smoke.spec.ts

test("portrait: cuộn → video compact; ‹ › đổi câu", async ({ page }) => {
  await login(page);
  await page.locator("a[href^='/lessons/']").first().click();
  await expect(page.getByTestId("video-frame")).toHaveAttribute("data-mode", "full");
  await page.mouse.wheel(0, 600); await page.waitForTimeout(300);
  await expect(page.getByTestId("video-frame")).toHaveAttribute("data-mode", "compact");
  await page.getByTestId("next-cue").click();
  await expect(page.locator("[data-testid=cue-item][data-active=true]")).toHaveCount(1);
});

test("landscape: dock ẩn, 2 cột", async ({ page }) => {
  await login(page);
  await page.setViewportSize({ width: 844, height: 390 });
  await page.locator("a[href^='/lessons/']").first().click();
  await expect(page.getByTestId("mobile-dock")).toBeHidden();
  await expect(page.getByTestId("cue-list")).toBeVisible();
});
```

Run: `pnpm --filter web typecheck && pnpm --filter web test && pnpm e2e`. Kiểm tay ở 375×812 và 844×390 (dev tools / Playwright screenshot). Commit: `git add -A apps/web/src e2e/mobile.spec.ts && git commit -m "feat(web): compact video on scroll, landscape two-column study layout, safe-area and manifest polish"`.

---

## Khối E — Service worker

### Task 14: `sw.js` + `/offline` + registrar + e2e PWA + docs

**Files:**
- Create: `apps/web/public/sw.js`
- Create: `apps/web/src/app/offline/page.tsx`
- Create: `apps/web/src/components/layout/ServiceWorkerRegistrar.tsx`
- Modify: `apps/web/src/app/layout.tsx`
- Create: `e2e/pwa.spec.ts`
- Modify: `CLAUDE.md` (lệnh e2e PWA, ghi chú SW)

- [x] **Step 1: `sw.js`**

```js
// Vingo service worker — viết tay (spec P2 §5, P2-6). Đổi VERSION mỗi lần sửa file này.
const VERSION = "v1";
const CACHE = `vingo-${VERSION}`;
const PRECACHE = ["/offline", "/manifest.webmanifest", "/icon-192.png", "/icon-512.png"];
const SUPABASE = new URL(self.location.href).searchParams.get("supabase") || "";
const NAV_TIMEOUT_MS = 4000;

self.addEventListener("install", (e) => {
  e.waitUntil(caches.open(CACHE).then((c) => c.addAll(PRECACHE)).then(() => self.skipWaiting()));
});
self.addEventListener("activate", (e) => {
  e.waitUntil(caches.keys().then((keys) => Promise.all(keys.filter((k) => k !== CACHE).map((k) => caches.delete(k)))).then(() => self.clients.claim()));
});

const isStatic = (u) => u.origin === self.location.origin && (u.pathname.startsWith("/_next/static/") || /^\/icon-\d+\.png$/.test(u.pathname));
const isFont = (u) => u.hostname === "fonts.gstatic.com";
const skip = (req, u) =>
  req.method !== "GET" || req.headers.has("range") ||
  (SUPABASE && u.href.startsWith(SUPABASE)) || u.pathname.startsWith("/auth/") ||
  u.hostname.endsWith("ytimg.com") || u.hostname.endsWith("youtube.com") ||
  (u.origin !== self.location.origin && !isFont(u));

async function cacheFirst(req) {
  const hit = await caches.match(req); if (hit) return hit;
  const res = await fetch(req);
  if (res.ok) (await caches.open(CACHE)).put(req, res.clone());
  return res;
}
async function navigate(req) {
  const cache = await caches.open(CACHE);
  try {
    const res = await Promise.race([fetch(req), new Promise((_, rej) => setTimeout(() => rej(new Error("timeout")), NAV_TIMEOUT_MS))]);
    if (res.ok) cache.put(req, res.clone());
    return res;
  } catch {
    return (await cache.match(req)) || (await cache.match("/offline"));
  }
}

self.addEventListener("fetch", (e) => {
  const u = new URL(e.request.url);
  if (skip(e.request, u)) return;
  if (e.request.mode === "navigate") { e.respondWith(navigate(e.request)); return; }
  if (isStatic(u) || isFont(u)) e.respondWith(cacheFirst(e.request));
});
```

- [x] **Step 2: `/offline`**

```tsx
// apps/web/src/app/offline/page.tsx — trang fallback khi mất mạng; tĩnh, không gọi Supabase.
import { Icon } from "@/components/ui/Icon";
export const dynamic = "force-static";
export default function OfflinePage() {
  return (
    <main className="grid min-h-screen place-items-center bg-page px-6 text-center text-ink">
      <div className="flex flex-col items-center gap-3">
        <Icon name="video-off" className="size-10 text-muted" />
        <h1 className="text-xl font-bold">Không có kết nối</h1>
        <p className="text-sm text-muted">Vingo cần mạng để tải bài và lưu tiến độ. Kiểm tra kết nối rồi thử lại.</p>
        <a href="/" className="mt-2 inline-flex min-h-[40px] items-center rounded-full bg-accent px-4 text-sm font-semibold text-accent-fg">Thử lại</a>
      </div>
    </main>
  );
}
```
`middleware.ts`: đảm bảo `/offline` không bị redirect về `/login` (thêm vào matcher loại trừ nếu middleware redirect chưa đăng nhập).

- [x] **Step 3: Registrar**

```tsx
// apps/web/src/components/layout/ServiceWorkerRegistrar.tsx — chỉ production (spec P2 §5.3).
"use client";
import { useEffect, useState } from "react";
export function ServiceWorkerRegistrar() {
  const [updateReady, setUpdateReady] = useState(false);
  useEffect(() => {
    if (process.env.NODE_ENV !== "production" || !("serviceWorker" in navigator)) return;
    const url = `/sw.js?supabase=${encodeURIComponent(process.env.NEXT_PUBLIC_SUPABASE_URL ?? "")}`;
    navigator.serviceWorker.register(url).then((reg) => {
      reg.addEventListener("updatefound", () => {
        const w = reg.installing;
        w?.addEventListener("statechange", () => { if (w.state === "installed" && navigator.serviceWorker.controller) setUpdateReady(true); });
      });
      const onVis = () => { if (document.visibilityState === "visible") void reg.update(); };
      document.addEventListener("visibilitychange", onVis);
    }).catch(() => { /* SW không bắt buộc */ });
  }, []);
  if (!updateReady) return null;
  return (
    <div role="status" className="fixed bottom-[96px] left-1/2 z-30 flex -translate-x-1/2 items-center gap-3 rounded-full bg-ink px-4 py-2 text-sm text-page shadow-card lg:bottom-6">
      Có bản mới <button type="button" className="font-semibold underline" onClick={() => location.reload()}>Tải lại</button>
    </div>
  );
}
```
`app/layout.tsx`: `<body …>{children}<ServiceWorkerRegistrar /></body>`.

- [x] **Step 4: e2e** `e2e/pwa.spec.ts` (chạy trên production build):

```ts
import { expect, test } from "@playwright/test";
test.skip(!process.env.E2E_PWA, "cần next build && next start (E2E_PWA=1)");
test("đăng ký SW và phục vụ /offline khi mất mạng", async ({ page, context }) => {
  await page.goto("/login");
  await page.waitForFunction(() => navigator.serviceWorker?.controller != null, null, { timeout: 15_000 });
  await context.setOffline(true);
  await page.goto("/dictionary").catch(() => {});
  await expect(page.getByText("Không có kết nối")).toBeVisible();
  await context.setOffline(false);
});
```

- [x] **Step 5: Chạy** — dừng dev server; `pnpm --filter web build && pnpm --filter web start` (cổng 3000) rồi `E2E_PWA=1 pnpm e2e -- pwa.spec.ts` → PASS. Mở DevTools > Application: SW active, cache `vingo-v1` có `/offline`. Tắt `next start`, bật lại dev server.

- [x] **Step 6: CLAUDE.md** — thêm vào mục Lệnh: `E2E_PWA=1 pnpm e2e -- pwa.spec.ts  # cần next build + next start, KHÔNG chạy cùng dev server`; mục Kiến trúc thêm đoạn ngắn: SW viết tay `public/sw.js` (đổi `VERSION` khi sửa), chỉ đăng ký production, không cache Supabase/video; provider `local`; `VideoFrame`/`YouTubePlayerAdapter`; variant `land:`. Cập nhật dòng Trạng thái: P2 code xong.

- [x] **Step 7: Commit** — `git add -A apps/web e2e/pwa.spec.ts CLAUDE.md && git commit -m "feat(web): hand-written service worker with offline fallback page; docs: P2 status"`

---

## Hoàn tất
- [x] `pnpm -r test && pnpm --filter web typecheck && pnpm --filter mcp typecheck && supabase test db && pnpm e2e` xanh.
- [ ] Kiểm tay trên điện thoại thật (Cát): tạo bài local từ skill → chọn file từ Files/Photos trên iOS → phát; bài YouTube không autoplay; Add to Home Screen; xoay ngang.
- [x] `docs/superpowers/specs/2026-09-08-vingo-p2-design.md` §1.3 điều kiện xong đã đạt → ghi "Trạng thái: đã triển khai" đầu spec.
