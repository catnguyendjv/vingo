# Vingo P1.5 — SRS Implementation Plan

> **Cách thực thi:** theo CLAUDE.md, superpowers chỉ dùng cho plan; plan này được triển khai
> **trực tiếp trong session** (không subagent-driven-development). Bước dùng checkbox `- [ ]`.

**Goal:** Người học đưa từ vựng vào ôn tập FSRS từ 3 chỗ (flashcard, cả bài, từ điển) và ôn trên
`/review` với câu ví dụ từ video, 4 nút chấm + Undo, badge số thẻ đến hạn trên header.

**Architecture:** `ts-fsrs` 5.x chạy trên client (`lib/srs.ts` thuần, có unit test); Postgres giữ
tính nguyên tử bằng 5 RPC nhỏ `security invoker` (RLS per-user là chốt quyền) + trigger đồng bộ
`known_words → review_cards.suspended`. `/review` là server component nạp `review_queue()` rồi
giao cho `ReviewSession` (client, giữ toàn bộ state) theo đúng pattern StudyView.

**Tech Stack:** Next.js 15 app router, Supabase (RLS, plpgsql, pgTAP), ts-fsrs ^5.4.2, Tailwind v4
token, Vitest (+jsdom), Playwright.

**Spec:** `docs/superpowers/specs/2026-09-08-vingo-p15-srs-design.md`

## Global Constraints
- `ts-fsrs` pin `^5.4.2` (đã cài ở `apps/web`), tham số mặc định + `enable_fuzz: true`.
- Cap **20 thẻ New / 24 giờ trượt**; requeue trong phiên khi `due - now < 15 phút`.
- Không hard-code màu; token Tailwind (`bg-surface`, `text-muted`, `bg-accent-soft`…). Chuỗi tiếng
  Nhật cần `lang="ja"` + `font-jp`. Một cây DOM cho desktop/mobile, phân nhánh bằng `lg:`.
- `cn()` chỉ nối chuỗi; không truyền `hidden` vào component đã có base display class.
- Migration mới `0008_srs.sql`, không sửa migration đã áp. RPC mới phải `revoke … from public, anon`
  + `grant … to authenticated`. Sửa policy/RPC → pgTAP mới trong `supabase/tests`.
- Test file `.test.tsx` cần `// @vitest-environment jsdom` dòng đầu. Test e2e phải idempotent.
- Commit message tiếng Anh, prefix `feat/fix/test/chore/docs`.
- Lệnh chạy từ root: `pnpm --filter web test -- <file>`, `pnpm --filter web typecheck`,
  `supabase db reset`, `supabase test db`, `pnpm e2e` (cần dev server :3000 + Supabase local).

---

## File Structure

| File | Trách nhiệm |
|---|---|
| `supabase/migrations/0008_srs.sql` | cột `source_cue_id`, trigger sync, 5 RPC, `my_dictionary` v2, grants |
| `supabase/tests/006_srs.sql` | pgTAP cho toàn bộ mục trên |
| `apps/web/src/lib/format.ts` | thêm `formatInterval(ms)` |
| `apps/web/src/lib/srs.ts` (+ `.test.ts`) | map row ↔ Card, scheduleOptions, buildQueue, requeue, GRADES |
| `apps/web/src/lib/review-api.ts` | wrapper gọi RPC `review_card`/`undo_review`/`enroll_cards` (inject được khi test) |
| `apps/web/src/lib/types.ts` | thêm `ReviewStats`, `QueueRow`, `EnrollItem` |
| `apps/web/src/app/(app)/layout.tsx` | gọi `review_stats()`, truyền `reviewDue` |
| `apps/web/src/components/layout/AppHeader.tsx` | link Ôn tập + badge |
| `apps/web/src/app/(app)/review/page.tsx` | server: queue + stats → `ReviewSession` |
| `apps/web/src/components/review/ReviewSession.tsx` (+ `.test.tsx`) | state phiên ôn |
| `apps/web/src/components/review/ReviewCard.tsx` | thẻ lật (props only) |
| `apps/web/src/components/review/ReviewActions.tsx` | nút Hiện đáp án / 4 nút chấm |
| `apps/web/src/components/review/ReviewProgress.tsx` | thanh tiến độ + Undo |
| `apps/web/src/components/review/ReviewEmpty.tsx` | hai trạng thái rỗng |
| `apps/web/src/components/ui/Flashcard.tsx` (+ test) | props `inReview`, `onEnroll` |
| `apps/web/src/components/study/VocabPanel.tsx` | nút "Đưa cả bài vào ôn tập", truyền enroll xuống |
| `apps/web/src/components/study/StudyView.tsx` | state `reviewTerms`, `enrollTerm`, `enrollLesson`, toast |
| `apps/web/src/app/(app)/lessons/[id]/page.tsx` | nạp `initialReviewTerms` |
| `apps/web/src/components/dictionary/DictionaryTable.tsx` (+ page) | cột/chip Ôn tập |
| `e2e/smoke.spec.ts` | luồng enroll → badge → review → undo → dọn |
| `CLAUDE.md` | cập nhật trạng thái (P1 xong, P1.5 xong) |

---

### Task 1: Migration `0008_srs.sql` + pgTAP `006_srs.sql`

**Files:**
- Create: `supabase/migrations/0008_srs.sql`
- Create: `supabase/tests/006_srs.sql`

**Interfaces (Produces):**
- `enroll_cards(items jsonb) → table(created int, reactivated int)`; item `{lang, term, reading?, meaning?, lesson_id?, cue_id?}`
- `review_card(p_card_id uuid, p_rating smallint, p_card jsonb, p_log jsonb) → review_cards`
- `undo_review(p_card_id uuid, p_card jsonb) → review_cards`
- `review_stats() → table(due int, new_available int, new_last_24h int, next_due_at timestamptz)`
  (`next_due_at` = `min(due_at)` của card active chưa due, null nếu không có — bổ sung so với spec 2.6 để phục vụ màn "Xong phiên")
- `review_queue() → table(id, lang, term, reading, meaning, due_at, stability, difficulty, scheduled_days, learning_steps, reps, lapses, state, last_review_at, created_at, source_lesson_id, lesson_title, source_cue_id, cue_idx, cue_text_source, cue_text_target)`
- `my_dictionary()` thêm cột cuối `in_review boolean`
- `p_card` jsonb keys: `due_at, stability, difficulty, scheduled_days, learning_steps, reps, lapses, state, last_review_at`

- [ ] **Step 1: Viết pgTAP trước** — `supabase/tests/006_srs.sql`

```sql
begin;
select plan(22);

insert into auth.users (instance_id, id, aud, role, email, encrypted_password, email_confirmed_at, raw_app_meta_data, raw_user_meta_data, created_at, updated_at) values
 ('00000000-0000-0000-0000-000000000000','11111111-0000-4000-8000-000000000001','authenticated','authenticated','a@t.local','',now(),'{}','{}',now(),now()),
 ('00000000-0000-0000-0000-000000000000','22222222-0000-4000-8000-000000000002','authenticated','authenticated','b@t.local','',now(),'{}','{}',now(),now());

insert into public.lessons (id, owner_id, title, source_type, status) values
 ('aaaa0000-0000-4000-8000-00000000000a','11111111-0000-4000-8000-000000000001','bài A','zoom','ready'),
 ('cccc0000-0000-4000-8000-00000000000c','11111111-0000-4000-8000-000000000001','bài xoá','zoom','ready');
update public.lessons set deleted_at = now() where id = 'cccc0000-0000-4000-8000-00000000000c';
insert into public.cues (id, lesson_id, idx, start_ms, end_ms, text_source, text_target) values
 ('aaaa1111-0000-4000-8000-000000000001','aaaa0000-0000-4000-8000-00000000000a',1,0,1000,'決済を確認します','xác nhận thanh toán'),
 ('cccc1111-0000-4000-8000-000000000001','cccc0000-0000-4000-8000-00000000000c',1,0,1000,'x','y');
insert into public.vocab_items (id, lesson_id, cue_id, term, reading, meaning) values
 ('aaaa2222-0000-4000-8000-000000000001','aaaa0000-0000-4000-8000-00000000000a','aaaa1111-0000-4000-8000-000000000001','決済','けっさい','thanh toán'),
 ('aaaa2222-0000-4000-8000-000000000002','aaaa0000-0000-4000-8000-00000000000a','aaaa1111-0000-4000-8000-000000000001','確認','かくにん','xác nhận');
insert into public.known_words (user_id, lang, term) values ('11111111-0000-4000-8000-000000000001','ja','決済');

-- Đóng vai A
select set_config('request.jwt.claims','{"sub":"11111111-0000-4000-8000-000000000001","role":"authenticated"}', true);
set local role authenticated;

-- enroll: 1 từ đã thuộc + 1 từ thường
select results_eq(
  $$select created, reactivated from public.enroll_cards('[
     {"lang":"ja","term":"決済","reading":"けっさい","meaning":"thanh toán","lesson_id":"aaaa0000-0000-4000-8000-00000000000a","cue_id":"aaaa1111-0000-4000-8000-000000000001"},
     {"lang":"ja","term":"確認","reading":"かくにん","meaning":"xác nhận","lesson_id":"aaaa0000-0000-4000-8000-00000000000a","cue_id":"aaaa1111-0000-4000-8000-000000000001"},
     {"lang":"ja","term":""}]'::jsonb)$$,
  $$values (2, 0)$$, 'enroll tạo 2 card, bỏ item thiếu term');
select is((select count(*) from public.known_words where term='決済'), 0::bigint, 'enroll gỡ known_words');
select is((select suspended from public.review_cards where term='決済'), false, 'card mới không suspended');
select is((select source_cue_id from public.review_cards where term='決済'), 'aaaa1111-0000-4000-8000-000000000001'::uuid, 'lưu source_cue_id');
select results_eq(
  $$select created, reactivated from public.enroll_cards('[{"lang":"ja","term":"決済"}]'::jsonb)$$,
  $$values (0, 1)$$, 'enroll lại → reactivated, không tạo trùng');

-- stats/queue ban đầu: 2 New, 0 due
select results_eq($$select due, new_available, new_last_24h from public.review_stats()$$, $$values (0, 2, 0)$$, 'stats: 2 New');
select is((select count(*) from public.review_queue()), 2::bigint, 'queue có 2 thẻ New');
select is((select cue_text_source from public.review_queue() where term='決済'), '決済を確認します', 'queue join câu ví dụ');
select is((select lesson_title from public.review_queue() where term='決済'), 'bài A', 'queue join tên bài');

-- review_card: card + log cùng lúc
create temp table c1 as select id from public.review_cards where term='決済';
select lives_ok($$select public.review_card((select id from c1), 3::smallint,
  '{"due_at":"2099-01-01T00:00:00Z","stability":3.1,"difficulty":5.2,"scheduled_days":3,"learning_steps":0,"reps":1,"lapses":0,"state":"Review","last_review_at":"2026-09-08T00:00:00Z"}'::jsonb,
  '{"rating":3,"state":0,"due":"2026-09-08T00:00:00Z","stability":0,"difficulty":0,"elapsed_days":0,"last_elapsed_days":0,"scheduled_days":0,"learning_steps":0,"review":"2026-09-08T00:00:00Z"}'::jsonb)$$,
  'review_card chạy được');
select is((select state from public.review_cards where id=(select id from c1)), 'Review', 'card cập nhật state');
select is((select count(*) from public.review_logs where card_id=(select id from c1)), 1::bigint, 'log được ghi');
select is((select new_last_24h from public.review_stats()), 0, 'log review ngày cũ không tính vào 24h');
update public.review_logs set reviewed_at = now() where card_id=(select id from c1);
select is((select new_last_24h from public.review_stats()), 1, 'log state=0 trong 24h được đếm');
select is((select count(*) from public.review_queue()), 1::bigint, 'thẻ đã review xa hạn rời queue');

-- undo: xoá log mới nhất, trả snapshot
select lives_ok($$select public.undo_review((select id from c1),
  '{"due_at":"2026-09-08T00:00:00Z","stability":0,"difficulty":0,"scheduled_days":0,"learning_steps":0,"reps":0,"lapses":0,"state":"New","last_review_at":null}'::jsonb)$$, 'undo chạy được');
select is((select count(*) from public.review_logs where card_id=(select id from c1)), 0::bigint, 'undo xoá log');
select is((select state from public.review_cards where id=(select id from c1)), 'New', 'undo trả state');
select throws_ok($$select public.undo_review((select id from c1), '{}'::jsonb)$$, 'P0002', null, 'undo khi không còn log → lỗi');

-- trigger known_words ↔ suspended
insert into public.known_words (user_id, lang, term) values ('11111111-0000-4000-8000-000000000001','ja','確認');
select is((select suspended from public.review_cards where term='確認'), true, 'đánh dấu đã thuộc → suspended');
select is((select in_review from public.my_dictionary() where term='確認'), false, 'my_dictionary.in_review=false khi suspended');
delete from public.known_words where term='確認';
select is((select suspended from public.review_cards where term='確認'), false, 'bỏ đánh dấu → unsuspended');

-- cap 20 New
insert into public.review_cards (user_id, lang, term)
  select '11111111-0000-4000-8000-000000000001','ja','w'||g from generate_series(1,25) g;
select is((select count(*) from public.review_queue() where state='New'), 20::bigint, 'queue tối đa 20 New (chưa review New nào trong 24h)');

-- Đóng vai B: không đụng được card của A
select set_config('request.jwt.claims','{"sub":"22222222-0000-4000-8000-000000000002","role":"authenticated"}', true);
select throws_ok($$select public.review_card((select id from c1), 3::smallint, '{}'::jsonb, '{}'::jsonb)$$, 'P0002', null, 'B không review được card của A');
select is((select count(*) from public.review_queue()), 0::bigint, 'queue của B rỗng');

select * from finish();
rollback;
```

(Đếm lại `plan(N)` cho khớp số assertion khi triển khai — hiện 24 assertion, đặt `plan(24)`.)

- [ ] **Step 2: Chạy để thấy fail**: `supabase test db` → fail vì hàm chưa tồn tại.

- [ ] **Step 3: Viết migration** — `supabase/migrations/0008_srs.sql`

```sql
-- P1.5 SRS (spec 2026-09-08-vingo-p15-srs-design.md §2).
alter table public.review_cards
  add column source_cue_id uuid references public.cues(id) on delete set null;

-- §2.2 known_words → review_cards.suspended (chạy dưới RLS của user đang ghi).
create function public.sync_known_to_cards() returns trigger
language plpgsql security invoker set search_path = public as $$
begin
  if tg_op = 'INSERT' then
    update review_cards set suspended = true
      where user_id = new.user_id and lang = new.lang and term = new.term and suspended = false;
    return new;
  end if;
  update review_cards set suspended = false
    where user_id = old.user_id and lang = old.lang and term = old.term and suspended = true;
  return old;
end $$;
create trigger known_words_sync_cards
after insert or delete on public.known_words
for each row execute function public.sync_known_to_cards();

-- §2.3
create function public.enroll_cards(items jsonb)
returns table(created int, reactivated int)
language plpgsql security invoker set search_path = public as $$
declare
  uid uuid := (select auth.uid());
  it jsonb; c int := 0; r int := 0; was_insert boolean;
begin
  if uid is null then raise exception 'not authenticated' using errcode = '42501'; end if;
  for it in select * from jsonb_array_elements(coalesce(items, '[]'::jsonb)) loop
    continue when nullif(it->>'lang', '') is null or nullif(it->>'term', '') is null;
    delete from known_words where user_id = uid and lang = it->>'lang' and term = it->>'term';
    insert into review_cards (user_id, lang, term, reading, meaning, source_lesson_id, source_cue_id, due_at)
    values (uid, it->>'lang', it->>'term', it->>'reading', it->>'meaning',
            (it->>'lesson_id')::uuid, (it->>'cue_id')::uuid, now())
    on conflict (user_id, lang, term) do update set suspended = false
    returning (xmax = 0) into was_insert;
    if was_insert then c := c + 1; else r := r + 1; end if;
  end loop;
  created := c; reactivated := r;
  return next;
end $$;

-- §2.4
create function public.review_card(p_card_id uuid, p_rating smallint, p_card jsonb, p_log jsonb)
returns public.review_cards
language plpgsql security invoker set search_path = public as $$
declare uid uuid := (select auth.uid()); rc public.review_cards;
begin
  if p_rating is null or p_rating not between 1 and 4 then
    raise exception 'invalid rating' using errcode = '22023';
  end if;
  update review_cards set
    due_at = (p_card->>'due_at')::timestamptz,
    stability = (p_card->>'stability')::real,
    difficulty = (p_card->>'difficulty')::real,
    scheduled_days = (p_card->>'scheduled_days')::int,
    learning_steps = (p_card->>'learning_steps')::int,
    reps = (p_card->>'reps')::int,
    lapses = (p_card->>'lapses')::int,
    state = p_card->>'state',
    last_review_at = (p_card->>'last_review_at')::timestamptz
  where id = p_card_id and user_id = uid
  returning * into rc;
  if rc.id is null then raise exception 'card not found' using errcode = 'P0002'; end if;
  insert into review_logs (card_id, user_id, rating, reviewed_at, log)
  values (p_card_id, uid, p_rating, coalesce((p_log->>'review')::timestamptz, now()), p_log);
  return rc;
end $$;

-- §2.5
create function public.undo_review(p_card_id uuid, p_card jsonb)
returns public.review_cards
language plpgsql security invoker set search_path = public as $$
declare uid uuid := (select auth.uid()); rc public.review_cards; lid uuid;
begin
  select id into lid from review_logs
    where card_id = p_card_id and user_id = uid
    order by reviewed_at desc, id desc limit 1;
  if lid is null then raise exception 'no review to undo' using errcode = 'P0002'; end if;
  delete from review_logs where id = lid;
  update review_cards set
    due_at = (p_card->>'due_at')::timestamptz,
    stability = (p_card->>'stability')::real,
    difficulty = (p_card->>'difficulty')::real,
    scheduled_days = (p_card->>'scheduled_days')::int,
    learning_steps = (p_card->>'learning_steps')::int,
    reps = (p_card->>'reps')::int,
    lapses = (p_card->>'lapses')::int,
    state = p_card->>'state',
    last_review_at = (p_card->>'last_review_at')::timestamptz
  where id = p_card_id and user_id = uid
  returning * into rc;
  if rc.id is null then raise exception 'card not found' using errcode = 'P0002'; end if;
  return rc;
end $$;

-- §2.6 (+ next_due_at cho màn "Xong phiên")
create function public.review_stats()
returns table(due int, new_available int, new_last_24h int, next_due_at timestamptz)
language sql stable security invoker set search_path = public as $$
  select
    (select count(*) from review_cards
      where user_id = (select auth.uid()) and not suspended and state <> 'New' and due_at <= now())::int,
    (select count(*) from review_cards
      where user_id = (select auth.uid()) and not suspended and state = 'New')::int,
    (select count(*) from review_logs
      where user_id = (select auth.uid()) and reviewed_at > now() - interval '24 hours'
        and log->>'state' = '0')::int,
    (select min(due_at) from review_cards
      where user_id = (select auth.uid()) and not suspended and due_at > now());
$$;

-- §2.7
create function public.review_queue()
returns table(
  id uuid, lang text, term text, reading text, meaning text,
  due_at timestamptz, stability real, difficulty real, scheduled_days int, learning_steps int,
  reps int, lapses int, state text, last_review_at timestamptz, created_at timestamptz,
  source_lesson_id uuid, lesson_title text, source_cue_id uuid, cue_idx int,
  cue_text_source text, cue_text_target text
) language sql stable security invoker set search_path = public as $$
  with picked as (
    select rc.* from review_cards rc
      where rc.user_id = (select auth.uid()) and not rc.suspended
        and rc.state <> 'New' and rc.due_at <= now()
    union all
    (select rc.* from review_cards rc
      where rc.user_id = (select auth.uid()) and not rc.suspended and rc.state = 'New'
      order by rc.created_at asc
      limit greatest(0, 20 - (select s.new_last_24h from review_stats() s)))
  )
  select p.id, p.lang, p.term, p.reading, p.meaning,
         p.due_at, p.stability, p.difficulty, p.scheduled_days, p.learning_steps,
         p.reps, p.lapses, p.state, p.last_review_at, p.created_at,
         p.source_lesson_id, l.title, p.source_cue_id, c.idx, c.text_source, c.text_target
  from picked p
  left join lessons l on l.id = p.source_lesson_id and l.deleted_at is null
  left join cues c on c.id = p.source_cue_id
  order by (p.state = 'New'), p.due_at, p.created_at;
$$;

-- §2.8 my_dictionary v2: thêm in_review (đổi kiểu trả về → drop/create).
drop function public.my_dictionary();
create function public.my_dictionary()
returns table (
  lang text, term text, reading text, meaning text,
  occurrences bigint, lesson_ids uuid[], cue_ids uuid[], is_known boolean, in_review boolean
) language sql stable set search_path = public as $$
  with accessible as (
    select l.id, l.source_lang
    from lessons l
    where l.deleted_at is null
      and (l.owner_id = (select auth.uid())
           or exists (select 1 from cue_progress cp
                      where cp.user_id = (select auth.uid()) and cp.lesson_id = l.id))
  ), v as (
    select a.source_lang as lang, vi.term, vi.reading, vi.meaning, vi.lesson_id, vi.cue_id,
           row_number() over (partition by a.source_lang, vi.term order by vi.created_at desc) as rn
    from vocab_items vi join accessible a on a.id = vi.lesson_id
  )
  select v.lang, v.term,
         max(v.reading) filter (where v.rn = 1) as reading,
         max(v.meaning) filter (where v.rn = 1) as meaning,
         count(*)::bigint as occurrences,
         array_agg(distinct v.lesson_id) as lesson_ids,
         array_agg(v.cue_id) as cue_ids,
         exists (select 1 from known_words k
                 where k.user_id = (select auth.uid()) and k.lang = v.lang and k.term = v.term) as is_known,
         exists (select 1 from review_cards rc
                 where rc.user_id = (select auth.uid()) and rc.lang = v.lang and rc.term = v.term
                   and rc.suspended = false) as in_review
  from v group by v.lang, v.term;
$$;

-- Grants (mẫu 0006).
revoke execute on function
  public.sync_known_to_cards(), public.enroll_cards(jsonb),
  public.review_card(uuid, smallint, jsonb, jsonb), public.undo_review(uuid, jsonb),
  public.review_stats(), public.review_queue(), public.my_dictionary()
  from public, anon;
grant execute on function
  public.enroll_cards(jsonb), public.review_card(uuid, smallint, jsonb, jsonb),
  public.undo_review(uuid, jsonb), public.review_stats(), public.review_queue(), public.my_dictionary()
  to authenticated;
```

- [ ] **Step 4: Áp migration và chạy test**: `supabase db reset` rồi `supabase test db` → tất cả pass (kể cả 001–005 cũ; `002_dictionary.sql` vẫn pass vì chỉ thêm cột cuối).

- [ ] **Step 5: Commit**
```bash
git add supabase/migrations/0008_srs.sql supabase/tests/006_srs.sql
git commit -m "feat(db): SRS RPCs, known_words sync trigger, in_review in my_dictionary"
```

---

### Task 2: `lib/srs.ts`, `formatInterval`, types

**Files:**
- Modify: `apps/web/src/lib/format.ts` (thêm `formatInterval`), `apps/web/src/lib/format.test.ts`
- Modify: `apps/web/src/lib/types.ts`
- Create: `apps/web/src/lib/srs.ts`, `apps/web/src/lib/srs.test.ts`

**Interfaces (Produces):**
```ts
// types.ts
export type StateName = "New" | "Learning" | "Review" | "Relearning";
export type CardRow = {
  id: string; lang: string; term: string; reading: string | null; meaning: string | null;
  due_at: string; stability: number | null; difficulty: number | null;
  scheduled_days: number | null; learning_steps: number | null; reps: number; lapses: number;
  state: StateName; last_review_at: string | null; created_at: string;
};
export type QueueRow = CardRow & {
  source_lesson_id: string | null; lesson_title: string | null; source_cue_id: string | null;
  cue_idx: number | null; cue_text_source: string | null; cue_text_target: string | null;
};
export type ReviewStats = { due: number; new_available: number; new_last_24h: number; next_due_at: string | null };
export type EnrollItem = { lang: string; term: string; reading: string | null; meaning: string | null; lesson_id: string | null; cue_id: string | null };
export type CardPatch = {
  due_at: string; stability: number; difficulty: number; scheduled_days: number; learning_steps: number;
  reps: number; lapses: number; state: StateName; last_review_at: string | null;
};
// srs.ts
export const NEW_CAP = 20; export const REQUEUE_WINDOW_MS = 15 * 60_000;
export const GRADES: { rating: 1|2|3|4; key: "1"|"2"|"3"|"4"; label: string }[]; // Quên/Khó/Được/Dễ
export function toFsrsCard(row: CardRow, now: Date): Card;
export function fromFsrsCard(card: Card): CardPatch;
export function patchOf(row: CardRow): CardPatch;          // snapshot Undo
export function serializeLog(log: ReviewLog): Record<string, unknown>;
export type ScheduleOption = { rating: 1|2|3|4; label: string; key: string; patch: CardPatch; log: Record<string, unknown>; dueAt: Date; intervalLabel: string };
export function scheduleOptions(row: CardRow, now: Date): ScheduleOption[];
export function buildQueue<T extends CardRow>(rows: T[]): T[];
export function applyPatch<T extends CardRow>(row: T, patch: CardPatch): T;
export function shouldRequeue(patch: CardPatch, now: Date): boolean;
export function badgeCount(stats: ReviewStats): number;    // due + min(new_available, max(0, 20 - new_last_24h))
// format.ts
export function formatInterval(ms: number): string;
```

- [ ] **Step 1: Test `formatInterval`** — thêm vào `format.test.ts`
```ts
import { formatInterval } from "./format";
describe("formatInterval", () => {
  it("các mốc", () => {
    expect(formatInterval(30_000)).toBe("<1 phút");
    expect(formatInterval(10 * 60_000)).toBe("10 phút");
    expect(formatInterval(3 * 3_600_000)).toBe("3 giờ");
    expect(formatInterval(3 * 86_400_000)).toBe("3 ngày");
    expect(formatInterval(60 * 86_400_000)).toBe("2 tháng");
    expect(formatInterval(548 * 86_400_000)).toBe("1,5 năm");
    expect(formatInterval(-5)).toBe("<1 phút");
  });
});
```
- [ ] **Step 2: Chạy fail** `pnpm --filter web test -- src/lib/format.test.ts`
- [ ] **Step 3: Implement** trong `format.ts`
```ts
export function formatInterval(ms: number): string {
  const min = ms / 60_000;
  if (!Number.isFinite(min) || min < 1) return "<1 phút";
  if (min < 60) return `${Math.round(min)} phút`;
  const h = min / 60;
  if (h < 24) return `${Math.round(h)} giờ`;
  const d = h / 24;
  if (d < 30) return `${Math.round(d)} ngày`;
  if (d < 365) return `${Math.round(d / 30)} tháng`;
  const y = d / 365;
  const s = y >= 10 ? String(Math.round(y)) : (Math.round(y * 10) / 10).toString().replace(".", ",");
  return `${s} năm`;
}
```
- [ ] **Step 4: Chạy pass.**
- [ ] **Step 5: Thêm types** vào `types.ts` (khối ở Interfaces).
- [ ] **Step 6: Test `srs.ts`** — `srs.test.ts`
```ts
import { describe, expect, it } from "vitest";
import { State } from "ts-fsrs";
import type { CardRow, CardPatch } from "./types";
import { toFsrsCard, fromFsrsCard, patchOf, scheduleOptions, buildQueue, applyPatch, shouldRequeue, badgeCount, GRADES } from "./srs";

const now = new Date("2026-09-08T09:00:00Z");
const base: CardRow = {
  id: "c1", lang: "ja", term: "決済", reading: "けっさい", meaning: "thanh toán",
  due_at: "2026-09-08T08:00:00Z", stability: null, difficulty: null, scheduled_days: null, learning_steps: null,
  reps: 0, lapses: 0, state: "New", last_review_at: null, created_at: "2026-09-07T00:00:00Z",
};

describe("toFsrsCard/fromFsrsCard", () => {
  it("New: null → 0, elapsed_days = 0", () => {
    const c = toFsrsCard(base, now);
    expect(c.state).toBe(State.New);
    expect(c.stability).toBe(0); expect(c.elapsed_days).toBe(0); expect(c.last_review).toBeUndefined();
  });
  it("Review: elapsed_days suy từ last_review_at", () => {
    const c = toFsrsCard({ ...base, state: "Review", stability: 5, difficulty: 6, last_review_at: "2026-09-05T09:00:00Z" }, now);
    expect(c.state).toBe(State.Review); expect(c.elapsed_days).toBe(3);
  });
  it("round-trip giữ state chuỗi và ISO", () => {
    const p = fromFsrsCard(toFsrsCard({ ...base, state: "Learning", last_review_at: "2026-09-08T08:30:00Z" }, now));
    expect(p.state).toBe("Learning"); expect(p.due_at).toBe("2026-09-08T08:00:00.000Z"); expect(p.last_review_at).toBe("2026-09-08T08:30:00.000Z");
  });
  it("patchOf là snapshot đủ cột", () => {
    expect(Object.keys(patchOf(base)).sort()).toEqual(["difficulty","due_at","lapses","last_review_at","learning_steps","reps","scheduled_days","stability","state"]);
  });
});

describe("scheduleOptions", () => {
  it("4 lựa chọn theo thứ tự Again..Easy, due tăng dần, log.state=0 cho thẻ New", () => {
    const o = scheduleOptions(base, now);
    expect(o.map((x) => x.rating)).toEqual([1, 2, 3, 4]);
    expect(o.map((x) => x.label)).toEqual(GRADES.map((g) => g.label));
    for (let i = 1; i < 4; i++) expect(o[i].dueAt.getTime()).toBeGreaterThanOrEqual(o[i - 1].dueAt.getTime());
    expect(o[2].log.state).toBe(0); expect(typeof o[2].log.review).toBe("string");
    expect(o[2].patch.reps).toBe(1); expect(o[2].intervalLabel.length).toBeGreaterThan(0);
  });
});

describe("queue", () => {
  const due = (id: string, due_at: string): CardRow => ({ ...base, id, state: "Review", due_at });
  const nw = (id: string, created_at: string): CardRow => ({ ...base, id, created_at });
  it("buildQueue: due theo due_at rồi New theo created_at", () => {
    const q = buildQueue([nw("n2", "2026-09-02T00:00:00Z"), due("d2", "2026-09-08T07:00:00Z"), nw("n1", "2026-09-01T00:00:00Z"), due("d1", "2026-09-08T06:00:00Z")]);
    expect(q.map((x) => x.id)).toEqual(["d1", "d2", "n1", "n2"]);
  });
  it("shouldRequeue: < 15 phút → true", () => {
    const p = (dueAt: string): CardPatch => ({ ...patchOf(base), due_at: dueAt });
    expect(shouldRequeue(p("2026-09-08T09:10:00Z"), now)).toBe(true);
    expect(shouldRequeue(p("2026-09-08T09:15:00Z"), now)).toBe(false);
  });
  it("applyPatch ghi đè cột lịch, giữ term", () => {
    const r = applyPatch(base, { ...patchOf(base), state: "Learning", reps: 1 });
    expect(r.state).toBe("Learning"); expect(r.reps).toBe(1); expect(r.term).toBe("決済");
  });
  it("badgeCount", () => {
    expect(badgeCount({ due: 3, new_available: 50, new_last_24h: 5, next_due_at: null })).toBe(18);
    expect(badgeCount({ due: 0, new_available: 2, new_last_24h: 30, next_due_at: null })).toBe(0);
  });
});
```
- [ ] **Step 7: Chạy fail.**
- [ ] **Step 8: Implement `srs.ts`**
```ts
import { dateDiffInDays, fsrs, generatorParameters, Rating, State, type Card, type Grade, type ReviewLog } from "ts-fsrs";
import type { CardPatch, CardRow, ReviewStats, StateName } from "./types";
import { formatInterval } from "./format";

export const NEW_CAP = 20;
export const REQUEUE_WINDOW_MS = 15 * 60_000;
export const GRADES = [
  { rating: Rating.Again as 1, key: "1", label: "Quên" },
  { rating: Rating.Hard as 2, key: "2", label: "Khó" },
  { rating: Rating.Good as 3, key: "3", label: "Được" },
  { rating: Rating.Easy as 4, key: "4", label: "Dễ" },
] as const;

const f = fsrs(generatorParameters({ enable_fuzz: true }));
const stateOf = (s: StateName): State => State[s];
const nameOf = (s: State): StateName => State[s] as StateName;

export function toFsrsCard(row: CardRow, now: Date): Card {
  const last = row.last_review_at ? new Date(row.last_review_at) : undefined;
  const state = stateOf(row.state);
  return {
    due: new Date(row.due_at), stability: row.stability ?? 0, difficulty: row.difficulty ?? 0,
    elapsed_days: state === State.New || !last ? 0 : Math.max(0, dateDiffInDays(last, now)),
    scheduled_days: row.scheduled_days ?? 0, learning_steps: row.learning_steps ?? 0,
    reps: row.reps, lapses: row.lapses, state, last_review: last,
  };
}
export function fromFsrsCard(card: Card): CardPatch {
  return {
    due_at: card.due.toISOString(), stability: card.stability, difficulty: card.difficulty,
    scheduled_days: card.scheduled_days, learning_steps: card.learning_steps,
    reps: card.reps, lapses: card.lapses, state: nameOf(card.state),
    last_review_at: card.last_review ? card.last_review.toISOString() : null,
  };
}
export function patchOf(row: CardRow): CardPatch {
  return {
    due_at: row.due_at, stability: row.stability ?? 0, difficulty: row.difficulty ?? 0,
    scheduled_days: row.scheduled_days ?? 0, learning_steps: row.learning_steps ?? 0,
    reps: row.reps, lapses: row.lapses, state: row.state, last_review_at: row.last_review_at,
  };
}
export function serializeLog(log: ReviewLog): Record<string, unknown> {
  return { ...log, due: log.due.toISOString(), review: log.review.toISOString() };
}
export type ScheduleOption = {
  rating: 1 | 2 | 3 | 4; key: string; label: string;
  patch: CardPatch; log: Record<string, unknown>; dueAt: Date; intervalLabel: string;
};
export function scheduleOptions(row: CardRow, now: Date): ScheduleOption[] {
  const rec = f.repeat(toFsrsCard(row, now), now);
  return GRADES.map((g) => {
    const { card, log } = rec[g.rating as Grade];
    return { rating: g.rating, key: g.key, label: g.label, patch: fromFsrsCard(card), log: serializeLog(log), dueAt: card.due, intervalLabel: formatInterval(card.due.getTime() - now.getTime()) };
  });
}
export function buildQueue<T extends CardRow>(rows: T[]): T[] {
  const due = rows.filter((r) => r.state !== "New").sort((a, b) => a.due_at.localeCompare(b.due_at));
  const fresh = rows.filter((r) => r.state === "New").sort((a, b) => a.created_at.localeCompare(b.created_at));
  return [...due, ...fresh];
}
export function applyPatch<T extends CardRow>(row: T, patch: CardPatch): T { return { ...row, ...patch }; }
export function shouldRequeue(patch: CardPatch, now: Date): boolean {
  return new Date(patch.due_at).getTime() - now.getTime() < REQUEUE_WINDOW_MS;
}
export function badgeCount(s: ReviewStats): number {
  return s.due + Math.min(s.new_available, Math.max(0, NEW_CAP - s.new_last_24h));
}
```
Lưu ý: ISO string so sánh bằng `localeCompare` chỉ đúng khi cùng định dạng; Postgres trả `+00:00` còn JS trả `Z` — trong `buildQueue` dùng `Date.parse` thay `localeCompare` nếu test lệch.
- [ ] **Step 9: Chạy pass** `pnpm --filter web test -- src/lib/srs.test.ts src/lib/format.test.ts`
- [ ] **Step 10: Commit** `feat(web): srs scheduling helpers on ts-fsrs 5`

---

### Task 3: `review-api.ts`, header badge, layout

**Files:**
- Create: `apps/web/src/lib/review-api.ts`
- Modify: `apps/web/src/app/(app)/layout.tsx`, `apps/web/src/components/layout/AppHeader.tsx`

**Interfaces (Produces):**
```ts
export type ReviewApi = {
  review(cardId: string, rating: number, patch: CardPatch, log: Record<string, unknown>): Promise<void>;
  undo(cardId: string, patch: CardPatch): Promise<void>;
  enroll(items: EnrollItem[]): Promise<{ created: number; reactivated: number }>;
};
export function createReviewApi(supabase: SupabaseClient): ReviewApi;  // throw Error(message) khi RPC lỗi
```
`AppHeader({ reviewDue }: { reviewDue: number })`.

- [ ] **Step 1: `review-api.ts`**
```ts
import type { SupabaseClient } from "@supabase/supabase-js";
import type { CardPatch, EnrollItem } from "./types";
export type ReviewApi = { /* như trên */ };
export function createReviewApi(supabase: SupabaseClient): ReviewApi {
  const call = async <T>(fn: string, args: Record<string, unknown>): Promise<T> => {
    const { data, error } = await supabase.rpc(fn, args);
    if (error) throw new Error(error.message);
    return data as T;
  };
  return {
    review: (cardId, rating, patch, log) => call("review_card", { p_card_id: cardId, p_rating: rating, p_card: patch, p_log: log }).then(() => undefined),
    undo: (cardId, patch) => call("undo_review", { p_card_id: cardId, p_card: patch }).then(() => undefined),
    enroll: async (items) => {
      const rows = await call<{ created: number; reactivated: number }[]>("enroll_cards", { items });
      return rows?.[0] ?? { created: 0, reactivated: 0 };
    },
  };
}
```
- [ ] **Step 2: layout** — async, đọc stats:
```tsx
import { createServerSupabase } from "@/lib/supabase/server";
import { badgeCount } from "@/lib/srs";
import type { ReviewStats } from "@/lib/types";
export default async function AppLayout({ children }) {
  const supabase = await createServerSupabase();
  const { data } = await supabase.rpc("review_stats");
  const stats = (data as ReviewStats[] | null)?.[0];
  const reviewDue = stats ? badgeCount(stats) : 0;
  return (<div className="min-h-screen bg-page text-ink"><AppHeader reviewDue={reviewDue} />…</div>);
}
```
(Chưa đăng nhập → RPC trả lỗi/rỗng → 0; không throw.)
- [ ] **Step 3: AppHeader** — thêm trước link Từ điển:
```tsx
<Link href="/review" aria-label="Ôn tập" data-testid="review-nav" className="relative inline-flex items-center gap-2 rounded-full px-3.5 py-2 font-medium transition-colors hover:bg-surface-2">
  <Icon name="flip" className="size-[18px]" />
  <span className="hidden sm:inline"><span lang="ja" className="font-jp text-muted">復習</span> Ôn tập</span>
  {reviewDue > 0 && (
    <span data-testid="review-badge" className="inline-flex min-w-[20px] items-center justify-center rounded-full bg-accent px-1.5 py-0.5 text-[11px] font-bold leading-none tabular-nums text-accent-fg">
      {reviewDue > 99 ? "99+" : reviewDue}
    </span>
  )}
</Link>
```
- [ ] **Step 4: typecheck** `pnpm --filter web typecheck`; mở dev server, header hiện link, badge 0 → ẩn.
- [ ] **Step 5: Commit** `feat(web): review nav with due badge and review api wrapper`

---

### Task 4: `/review` — ReviewSession + con + test

**Files:**
- Create: `apps/web/src/app/(app)/review/page.tsx`
- Create: `apps/web/src/components/review/ReviewSession.tsx`, `ReviewCard.tsx`, `ReviewActions.tsx`, `ReviewProgress.tsx`, `ReviewEmpty.tsx`, `ReviewSession.test.tsx`

**Interfaces:**
- Consumes: `buildQueue, scheduleOptions, patchOf, applyPatch, shouldRequeue, GRADES` (Task 2); `ReviewApi` (Task 3).
- `ReviewSession({ initialQueue: QueueRow[]; stats: ReviewStats; hasAnyCard: boolean; api?: ReviewApi; now?: () => Date })`
- `ReviewCard({ row: QueueRow; revealed: boolean; onToggle(): void })`
- `ReviewActions({ revealed: boolean; options: ScheduleOption[] | null; pending: boolean; onReveal(): void; onRate(o: ScheduleOption): void })`
- `ReviewProgress({ done: number; total: number; dueLeft: number; newLeft: number; canUndo: boolean; pending: boolean; onUndo(): void })`
- `ReviewEmpty({ kind: "none" | "done"; reviewed: number; nextDueAt: string | null })`

- [ ] **Step 1: Test** `ReviewSession.test.tsx`
```tsx
// @vitest-environment jsdom
import { afterEach, describe, expect, it, vi } from "vitest";
import { cleanup, fireEvent, render, screen, waitFor } from "@testing-library/react";
import { ReviewSession } from "./ReviewSession";
import type { QueueRow, ReviewStats } from "@/lib/types";

const row = (id: string, term: string, extra: Partial<QueueRow> = {}): QueueRow => ({
  id, lang: "ja", term, reading: "よみ", meaning: `nghĩa ${term}`,
  due_at: "2026-09-08T08:00:00Z", stability: null, difficulty: null, scheduled_days: null, learning_steps: null,
  reps: 0, lapses: 0, state: "New", last_review_at: null, created_at: "2026-09-01T00:00:00Z",
  source_lesson_id: "L1", lesson_title: "Bài 1", source_cue_id: "C1", cue_idx: 3,
  cue_text_source: `${term}を確認します`, cue_text_target: "xác nhận", ...extra,
});
const stats: ReviewStats = { due: 0, new_available: 2, new_last_24h: 0, next_due_at: null };
const api = () => ({ review: vi.fn().mockResolvedValue(undefined), undo: vi.fn().mockResolvedValue(undefined), enroll: vi.fn() });
const now = () => new Date("2026-09-08T09:00:00Z");
afterEach(cleanup);

describe("ReviewSession", () => {
  it("rỗng: không có card nào → hướng dẫn", () => {
    render(<ReviewSession initialQueue={[]} stats={{ ...stats, new_available: 0 }} hasAnyCard={false} api={api()} now={now} />);
    expect(screen.getByTestId("review-empty").getAttribute("data-kind")).toBe("none");
  });
  it("lật bằng click và Space, 4 nút chỉ hiện sau khi lật", () => {
    render(<ReviewSession initialQueue={[row("a", "決済")]} stats={stats} hasAnyCard api={api()} now={now} />);
    expect(screen.queryByTestId("review-rating-3")).toBeNull();
    fireEvent.click(screen.getByTestId("review-card"));
    expect(screen.getByTestId("review-card").getAttribute("data-revealed")).toBe("true");
    expect(screen.getByText("決済を確認します")).toBeTruthy();
    expect(screen.getByRole("link", { name: /Mở trong bài/ }).getAttribute("href")).toBe("/lessons/L1#cueid=C1");
    expect(screen.getByTestId("review-rating-3")).toBeTruthy();
  });
  it("chấm Good → gọi api.review với patch reps=1, sang thẻ sau; Undo trả thẻ về", async () => {
    const a = api();
    render(<ReviewSession initialQueue={[row("a", "決済"), row("b", "確認")]} stats={stats} hasAnyCard api={a} now={now} />);
    fireEvent.keyDown(window, { key: " " });
    fireEvent.click(screen.getByTestId("review-rating-3"));
    await waitFor(() => expect(a.review).toHaveBeenCalledTimes(1));
    const [cardId, rating, patch] = a.review.mock.calls[0];
    expect(cardId).toBe("a"); expect(rating).toBe(3); expect(patch.reps).toBe(1);
    expect(screen.getByTestId("review-card").textContent).toContain("確認");
    expect(screen.getByTestId("review-progress").textContent).toContain("1 / 2");
    fireEvent.click(screen.getByTestId("review-undo"));
    await waitFor(() => expect(a.undo).toHaveBeenCalledWith("a", expect.objectContaining({ state: "New", reps: 0 })));
    expect(screen.getByTestId("review-card").textContent).toContain("決済");
    expect(screen.getByTestId("review-card").getAttribute("data-revealed")).toBe("false");
  });
  it("Again trên thẻ New requeue về cuối; hết queue → màn Xong", async () => {
    const a = api();
    render(<ReviewSession initialQueue={[row("a", "決済")]} stats={stats} hasAnyCard api={a} now={now} />);
    fireEvent.click(screen.getByTestId("review-card"));
    fireEvent.click(screen.getByTestId("review-rating-1"));          // Again → due ~1 phút → requeue
    await waitFor(() => expect(a.review).toHaveBeenCalledTimes(1));
    expect(screen.getByTestId("review-card").textContent).toContain("決済");
    fireEvent.click(screen.getByTestId("review-card"));
    fireEvent.click(screen.getByTestId("review-rating-4"));          // Easy → rời phiên
    await waitFor(() => expect(screen.getByTestId("review-empty").getAttribute("data-kind")).toBe("done"));
  });
  it("RPC lỗi → thẻ về đầu hàng đợi, có alert", async () => {
    const a = api(); a.review.mockRejectedValue(new Error("boom"));
    render(<ReviewSession initialQueue={[row("a", "決済"), row("b", "確認")]} stats={stats} hasAnyCard api={a} now={now} />);
    fireEvent.click(screen.getByTestId("review-card"));
    fireEvent.click(screen.getByTestId("review-rating-3"));
    await waitFor(() => expect(screen.getByRole("alert").textContent).toContain("boom"));
    expect(screen.getByTestId("review-card").textContent).toContain("決済");
  });
});
```
- [ ] **Step 2: Chạy fail.**
- [ ] **Step 3: Implement components**

`ReviewSession.tsx` — state: `queue`, `revealed`, `pending`, `reviewed`, `undo: {row: QueueRow (snapshot trước), } | null`, `error`. Handlers:
```tsx
const rate = async (o: ScheduleOption) => {
  const cur = queue[0]; if (!cur || pending) return;
  const rest = queue.slice(1);
  const updated = applyPatch(cur, o.patch);
  setUndo({ prev: cur }); setRevealed(false); setPending(true); setError(null);
  setQueue(shouldRequeue(o.patch, now()) ? [...rest, updated] : rest);
  setReviewed((n) => n + 1);
  try { await api.review(cur.id, o.rating, o.patch, o.log); }
  catch (e) { setError((e as Error).message); setQueue((q) => [cur, ...q.filter((r) => r.id !== cur.id)]); setReviewed((n) => n - 1); setUndo(null); }
  finally { setPending(false); }
};
const undoLast = async () => {
  if (!undo || pending) return;
  setPending(true);
  try { await api.undo(undo.prev.id, patchOf(undo.prev)); setQueue((q) => [undo.prev, ...q.filter((r) => r.id !== undo.prev.id)]); setReviewed((n) => Math.max(0, n - 1)); setRevealed(false); setUndo(null); }
  catch (e) { setError((e as Error).message); } finally { setPending(false); }
};
```
`options = useMemo(() => cur ? scheduleOptions(cur, now()) : null, [cur?.id, revealed])` — tính khi lật để `now` sát lúc chấm. Keyboard: `useEffect` gắn `keydown` trên `window`: Space → toggle reveal (preventDefault, bỏ qua khi target là input/button), `1–4` → rate khi revealed, `z` → undo. `total = reviewed + queue.length`; `dueLeft = queue.filter(s ≠ New).length`, `newLeft` phần còn lại. Khi `queue.length === 0`: `<ReviewEmpty kind={hasAnyCard || reviewed > 0 ? "done" : "none"} reviewed={reviewed} nextDueAt={stats.next_due_at} />`. Layout: `<main className="mx-auto flex max-w-[560px] flex-col gap-4 pb-[140px] lg:pb-0">`. `api` mặc định `useMemo(() => apiProp ?? createReviewApi(createClient()), [])`.

`ReviewCard.tsx` — `div.fc` với `data-testid="review-card" data-revealed`; click → `onToggle`. Mặt trước: chip trạng thái (`Badge kind="draft"` cho New "Mới", `learning` cho Learning/Relearning "Đang học"/"Học lại", `done` cho Review "Ôn"), term `text-[40px] lg:text-5xl font-jp lang="ja"`. Mặt sau: reading, meaning `text-lg`, khối câu ví dụ trong `rounded-md bg-surface-2 p-3` (chỉ khi `cue_text_source`), link `Mở trong bài` (`Link href={`/lessons/${source_lesson_id}#cueid=${source_cue_id}`}`, chỉ khi có `source_lesson_id && source_cue_id`), tên bài `text-muted text-xs`. Min-height 260px; dùng class `.fc-flipped` khi revealed (CSS đã có, hover không đổi vì thẻ này không có `fc:hover`? — CSS hiện có `.fc:hover` lật trên desktop; để tránh, dùng class riêng `rc`/`rc-flipped` copy 3 rule transform từ `.fc` trong `globals.css` **không** kèm rule hover).

`ReviewActions.tsx` — `div` `fixed inset-x-0 bottom-0 z-20 border-t border-line bg-surface p-3 pb-[calc(12px+env(safe-area-inset-bottom))] lg:static lg:border-0 lg:bg-transparent lg:p-0`. Chưa lật: `<Button variant="primary" className="w-full justify-center min-h-[48px]" onClick={onReveal}>Hiện đáp án <kbd>Space</kbd></Button>`. Đã lật: `grid grid-cols-4 gap-2`, mỗi nút `data-testid={`review-rating-${o.rating}`}` hiện `label` + `intervalLabel` (`text-[11px] text-muted`), `disabled={pending}`; Again dùng `text-danger`, Easy `text-success` qua class.

`ReviewProgress.tsx` — `data-testid="review-progress"`: `"{done} / {total}"` tabular, chips `còn {dueLeft} đến hạn · {newLeft} mới`, nút `IconButton label="Hoàn tác (Z)" data-testid="review-undo" disabled={!canUndo || pending}` icon `flip`.

`ReviewEmpty.tsx` — `data-testid="review-empty" data-kind={kind}`. `none`: tiêu đề "Chưa có từ nào để ôn", 3 dòng hướng dẫn, 2 `Link` (thư viện, từ điển). `done`: "Xong phiên hôm nay", "Đã ôn {reviewed} thẻ", nếu `nextDueAt` → "Thẻ tiếp theo đến hạn: {formatInterval(next - now)} nữa".

`page.tsx`:
```tsx
export default async function ReviewPage() {
  const supabase = await createServerSupabase();
  const { data: { user } } = await supabase.auth.getUser();
  if (!user) redirect("/login");
  const [{ data: queue }, { data: stats }, { count }] = await Promise.all([
    supabase.rpc("review_queue"), supabase.rpc("review_stats"),
    supabase.from("review_cards").select("id", { count: "exact", head: true }).eq("user_id", user.id),
  ]);
  return (<ReviewSession initialQueue={(queue ?? []) as QueueRow[]} stats={(stats as ReviewStats[])?.[0] ?? { due: 0, new_available: 0, new_last_24h: 0, next_due_at: null }} hasAnyCard={(count ?? 0) > 0} />);
}
```
- [ ] **Step 4: Chạy pass** `pnpm --filter web test -- src/components/review/ReviewSession.test.tsx`, `pnpm --filter web typecheck`.
- [ ] **Step 5: Thử tay** trên dev server: chưa có card → `none`; tạm insert card qua Studio → ôn được, mobile (375px) nút đáy không che thẻ.
- [ ] **Step 6: Commit** `feat(web): /review session with FSRS rating, undo and example sentence`

---

### Task 5: Enroll trên trang học (Flashcard, VocabPanel, StudyView, page)

**Files:**
- Modify: `apps/web/src/components/ui/Flashcard.tsx`, `Flashcard.test.tsx`
- Modify: `apps/web/src/components/study/VocabPanel.tsx`, `StudyView.tsx`, `apps/web/src/app/(app)/lessons/[id]/page.tsx`

**Interfaces:**
- `Flashcard` thêm `inReview?: boolean; onEnroll?: () => void`.
- `VocabPanel` thêm `reviewTerms: Set<string>; onEnroll: (v: VocabRow) => void; onEnrollAll: () => void; enrollableCount: number`.
- `StudyView` thêm prop `initialReviewTerms: string[]`.

- [ ] **Step 1: Test Flashcard** (thêm vào describe mobile):
```tsx
it("mặt sau có nút Ôn tập → onEnroll; inReview → chip Đang ôn", () => {
  const enroll = vi.fn();
  const { rerender } = render(<Flashcard {...base} onToggleKnown={() => {}} onEnroll={enroll} />);
  fireEvent.click(screen.getByRole("button", { name: "採用状況: đánh dấu đã thuộc" }));
  fireEvent.click(screen.getByTestId("flashcard-enroll"));
  expect(enroll).toHaveBeenCalledTimes(1);
  rerender(<Flashcard {...base} onToggleKnown={() => {}} onEnroll={enroll} inReview />);
  expect(screen.queryByTestId("flashcard-enroll")).toBeNull();
  expect(screen.getByText("Đang ôn")).toBeTruthy();
});
```
- [ ] **Step 2: Chạy fail; implement** trong `fc-actions` (trước nút Đóng):
```tsx
{onEnroll && (inReview
  ? <span className="rounded-full bg-accent-soft px-2 py-0.5 text-[11px] font-medium text-accent">Đang ôn</span>
  : <button type="button" data-testid="flashcard-enroll" tabIndex={revealed ? 0 : -1}
      onClick={(e) => { e.stopPropagation(); onEnroll(); closeBack(e.currentTarget); }}
      className="rounded-full border border-line bg-surface px-2 py-0.5 text-[11px] font-medium hover:border-muted">＋ Ôn tập</button>)}
```
Desktop (`hover: hover`) `.fc-actions` bị ẩn bằng CSS → thêm rule trong `globals.css`: `@media (hover: hover) { .fc-actions { display: flex; } .fc-actions [data-known-toggle] { display: none; } }` và gắn `data-known-toggle` vào nút "✓ Đã thuộc" (desktop click thẻ đã là đánh dấu). Kiểm tra lại test desktop hiện có vẫn pass.
- [ ] **Step 3: Chạy pass Flashcard test.**
- [ ] **Step 4: VocabPanel** — header thêm sau nút ẩn/hiện:
```tsx
<Button variant="soft" size="sm" data-testid="enroll-all" disabled={enrollableCount === 0} onClick={onEnrollAll}>
  <Icon name="flip" className="size-4" />Ôn tập cả bài{enrollableCount > 0 && ` · ${enrollableCount}`}
</Button>
```
và `card()` truyền `inReview={reviewTerms.has(v.term)} onEnroll={() => onEnroll(v)}`.
- [ ] **Step 5: StudyView**
```tsx
const api = useMemo(() => createReviewApi(supabase), [supabase]);
const [reviewTerms, setReviewTerms] = useState<Set<string>>(new Set(initialReviewTerms));
const [toast, setToast] = useState<string | null>(null);
const notify = (m: string) => { setToast(m); window.setTimeout(() => setToast(null), 2500); };
const itemOf = (v: VocabRow): EnrollItem => ({ lang: lesson.source_lang, term: v.term, reading: v.reading, meaning: v.meaning, lesson_id: lesson.id, cue_id: v.cue_id });
const enrollTerm = async (v: VocabRow) => {
  setReviewTerms((s) => new Set(s).add(v.term));
  setKnownTerms((s) => { const n = new Set(s); n.delete(v.term); return n; });
  try { await api.enroll([itemOf(v)]); notify(`Đã đưa「${v.term}」vào ôn tập`); }
  catch (e) { setReviewTerms((s) => { const n = new Set(s); n.delete(v.term); return n; }); notify(`Lỗi: ${(e as Error).message}`); }
};
const enrollable = useMemo(() => { const seen = new Set<string>(); return localVocab.filter((v) => !knownTerms.has(v.term) && !reviewTerms.has(v.term) && !seen.has(v.term) && seen.add(v.term)); }, [localVocab, knownTerms, reviewTerms]);
const enrollLesson = async () => {
  if (!enrollable.length) return notify("Không còn từ nào để thêm");
  setReviewTerms((s) => new Set([...s, ...enrollable.map((v) => v.term)]));
  try { const r = await api.enroll(enrollable.map(itemOf)); notify(`Đã thêm ${r.created} từ${r.reactivated ? `, ${r.reactivated} từ đã có` : ""}`); }
  catch (e) { notify(`Lỗi: ${(e as Error).message}`); }
};
```
`toggleKnown` khi đánh dấu known → `setReviewTerms` bỏ term. Toast: `{toast && <div role="status" className="fixed bottom-[96px] left-1/2 z-30 -translate-x-1/2 rounded-full bg-ink px-4 py-2 text-sm text-page shadow-card lg:bottom-6">{toast}</div>}`.
- [ ] **Step 6: page.tsx** — thêm vào `Promise.all`: `supabase.from("review_cards").select("term").eq("lang", lesson.source_lang).eq("user_id", user.id).eq("suspended", false)` → `initialReviewTerms`.
- [ ] **Step 7: typecheck + toàn bộ test web**; thử tay 3 đường enroll, badge header tăng.
- [ ] **Step 8: Commit** `feat(web): enroll vocab into review from flashcards and whole lesson`

---

### Task 6: Từ điển — cột/chip Ôn tập

**Files:**
- Modify: `apps/web/src/components/dictionary/DictionaryTable.tsx` (`DictEntry.in_review`, `Set inReview`, nút/chip), `apps/web/src/app/(app)/dictionary/page.tsx` (không đổi ngoài type)

- [ ] **Step 1: Test** `DictionaryTable.test.tsx` (mới, jsdom, mock `@/lib/supabase/client` bằng `vi.mock` trả `rpc` spy):
```tsx
// @vitest-environment jsdom
import { afterEach, describe, expect, it, vi } from "vitest";
import { cleanup, fireEvent, render, screen, waitFor } from "@testing-library/react";
const rpc = vi.fn().mockResolvedValue({ data: [{ created: 1, reactivated: 0 }], error: null });
vi.mock("@/lib/supabase/client", () => ({ createClient: () => ({ rpc, from: () => ({ delete: () => ({ match: vi.fn().mockResolvedValue({}) }), upsert: vi.fn().mockResolvedValue({}) }) }) }));
import { DictionaryTable, type DictEntry } from "./DictionaryTable";
const e = (term: string, x: Partial<DictEntry> = {}): DictEntry => ({ lang: "ja", term, reading: "r", meaning: "m", occurrences: 1, lesson_ids: ["L1"], cue_ids: ["C1"], is_known: false, in_review: false, ...x });
afterEach(cleanup);
it("nút Ôn tập gọi enroll_cards và chuyển thành chip Đang ôn", async () => {
  render(<DictionaryTable entries={[e("決済"), e("確認", { in_review: true })]} userId="u" />);
  expect(screen.getAllByText("Đang ôn").length).toBeGreaterThan(0);
  fireEvent.click(screen.getAllByTestId("dict-enroll")[0]);
  await waitFor(() => expect(rpc).toHaveBeenCalledWith("enroll_cards", expect.objectContaining({ items: [expect.objectContaining({ term: "決済", lesson_id: "L1", cue_id: "C1" })] })));
  expect(screen.getAllByText("Đang ôn").length).toBeGreaterThan(1);
});
```
- [ ] **Step 2: Chạy fail; implement**: `const api = useMemo(() => createReviewApi(supabase), [supabase])`; `inReview` Set; `enroll(e)`: optimistic add + bỏ known, gọi `api.enroll([{ lang, term, reading, meaning, lesson_id: e.lesson_ids[0] ?? null, cue_id: e.cue_ids[0] ?? null }])`, lỗi → revert. `toggleKnown` khi đánh dấu → bỏ khỏi `inReview`. Desktop thêm `<th>Ôn tập</th>` + `<td>` chứa `<Button size="sm" data-testid="dict-enroll">Ôn tập</Button>` hoặc chip; mobile thêm vào dòng `<p className="mt-2 flex …">`. CSV export không đổi.
- [ ] **Step 3: Pass + typecheck. Commit** `feat(web): enroll into review from dictionary with in-review chip`

---

### Task 7: e2e, docs, CLAUDE.md

**Files:**
- Modify: `e2e/smoke.spec.ts`, `CLAUDE.md`, `docs/superpowers/specs/2026-09-08-vingo-p15-srs-design.md` (ghi `next_due_at`)

- [ ] **Step 1: e2e** (desktop):
```ts
test("ôn tập: enroll từ trang học → badge → /review chấm → undo → dọn", async ({ page }) => {
  await login(page);
  await page.locator("a[href^='/lessons/']").first().click();
  await page.getByRole("tab", { name: /Tất cả/ }).click();
  const card = page.locator('[data-testid="flashcard"]:visible').first();
  const term = ((await card.locator(".fc-front span[lang='ja']").first().textContent()) ?? "").trim();
  await card.hover();
  await card.getByTestId("flashcard-enroll").click();
  await expect(card.getByText("Đang ôn")).toBeVisible();
  await page.goto("/review");
  await expect(page.getByTestId("review-badge")).toBeVisible();
  await page.getByTestId("review-card").click();
  await page.getByTestId("review-rating-3").click();
  await page.getByTestId("review-undo").click();
  await expect(page.getByTestId("review-card")).toHaveAttribute("data-revealed", "false");
  // dọn: xoá card qua trang từ điển không có API → dùng supabase client trong page context
  await page.goto("/dictionary");
  await page.evaluate(async (t) => {
    const { createClient } = await import("/_next/static/chunks/app/(app)/dictionary/page.js").catch(() => ({ createClient: null }));
    void createClient; void t;
  }, term);
});
```
Việc dọn bằng import chunk không ổn định → thay bằng: trước test, dùng `@supabase/supabase-js` trong Node (Playwright chạy Node) với `SUPABASE_URL`/`SERVICE_ROLE_KEY` từ env e2e (đã có cho `create-dev-user`), xoá `review_cards` của user `cat@vingo.local` với `term` sau test trong `test.afterEach`. Nếu env không có service key, `test.skip`.
- [ ] **Step 2: Chạy** `pnpm e2e` với dev server + Supabase local.
- [ ] **Step 3: CLAUDE.md**: sửa đoạn Trạng thái → "P0, P1 (study-kit-mcp localhost), P1.5 (SRS) code xong. Chưa làm: cloud provisioning/deploy (đã quyết định lùi), P2 (YouTube player, share community, service worker)". Thêm 1 dòng kiến trúc: "SRS: ts-fsrs chạy client (`lib/srs.ts`), ghi qua RPC `review_card/undo_review/enroll_cards`; trigger `known_words` ↔ `review_cards.suspended`; cap 20 New/24h trượt."
- [ ] **Step 4: Commit** `test(e2e): review flow; docs: update status for P1.5`

---

## Self-review
- **Spec coverage:** §2.1–2.9 → Task 1; §3 → Task 2; §4.1 → Task 3; §4.2 → Task 4; §4.3 → Task 5; §4.4 → Task 6; §5 → test trong từng task + Task 7. Bổ sung `next_due_at` (ghi lại vào spec ở Task 7).
- **Type consistency:** `CardPatch`/`QueueRow`/`ReviewStats`/`EnrollItem` định nghĩa ở Task 2, dùng ở 3–6 cùng tên; RPC arg names `p_card_id/p_rating/p_card/p_log`, `items` khớp migration.
- **Điểm cần kiểm khi làm:** `xmax = 0` trong `returning` của plpgsql; `.fc:hover` lật thẻ review ngoài ý muốn (dùng class riêng); ISO `+00:00` vs `Z` khi sort.
